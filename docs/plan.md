# Greenqubes — Build Plan

> Updated after each session. Read this alongside CONTEXT.md at the start of every session.

_Last updated: 2026-09-04 (feat-provision — **Provisioning overhaul LIVE on production for launch day.** Card-only users (name + role, no email — attach it later, first sign-in links up), subrole / is_driver / qualifications columns (migration **0052** — renumbered from 0051, which V3's job_projects had already claimed on the shared DB), link-status DOTS (red no-email / amber waiting / green linked) + Driver chip + subrole/qualification chips on admin rows and the installer + designer grids (dropdown pickers untouched), admin role+subrole filter bar incl. "No subrole", rename + email editing without delete-and-re-add, years/skills hidden everywhere (columns kept — drop reminder ledgered), FCFS panel + clash modal meta swapped, and the **Support crew bucket** — the sub-installer bucket retitled and widened to ALL roles, riding the existing `is_sub_installer` flag (Supporting-Role Telegram, clash-exempt, workload-counted; zero extra migration). Merged cleanly with the tour agent's parallel dev work, dev → main (fc9f13c), production probes green. New `user-meta` suite; 19 suites green.)_
_Last updated: 2026-09-04 (feat-tour — **Guided app tour LIVE on production** for all 6 roles in English/中文/বাংলা (Bengali = Nic's scoped exception to the bn freeze; both translations unvetted pending demo corrections): first-login welcome card → language chooser (sets the app language via the existing lang route — the tour's only write) → role-matched spotlight walkthrough closing on Connect Telegram; re-open via profile picture → App tour. Built subagent-driven from spec + approved interactive mockup; the final whole-branch review caught a stale-cross-role-state navigation loop, a 2s undimmed gap between steps and two data-dependent anchors — all fixed pre-ship. The go-live merge dev→main also carried the concurrent provisioning-overhaul agent's work (subroles/Driver/qualifications, migration 0052 — verified applied by a live-DB probe before merging, its checklist gate was unticked). Office Depot go-live day.)_
_Last updated: 2026-09-04 (feat-installer — **Installer completion flow LIVE on production.** Installers finish their own scheduled jobs: a Completed button on the job form (grey until a completion photo is uploaded, green after; confirm modal; new `/api/jobs/[id]/complete` route enforcing formal assignment + scheduled + ≥1 completion photo on the service client; ONE awaited Telegram to the sales POC — `tplJobCompleted`); "Signed DO (Optional)" hidden until production ticks DO issued (existing uploads always visible); migration **0053** — every installer can read ALL completed jobs (details/files/chat/tasks/formal crew) for back-reference, via a `job_is_completed()` SECURITY DEFINER helper that dodges the jobs↔job_assignees RLS recursion. Bonus from Nic's "button invisible in daylight" catch: 30 bare green/amber/blue Tailwind classes across 9 files had NEVER compiled (tokens are `brand-*`) — all renamed, restoring long-missing tints (clash-modal badges, workload-modal day circle, bug severities, assistant pins, preview-as ring). New completion-rules suite; 20 suites green; dev → main `edeaf62`, production probes green.)_

---

## Table of Contents

- [Current State](#current-state)
- [Completed Sessions](#completed-sessions)
- [Session 19 — Pre-Alpha Testing (PASSED 2026-08-17)](#session-19)
- [Session 20 — Pre-Alpha Feedback (skipped — clean pass)](#session-20)
- [Sessions 21–23 — closed: webapp declared launched v1.0.0](#sessions-21-23)
- [Mobile App — the new roadmap](#mobile-app)
- [Environment Status](#environment-status)

---

## Current State

**The webapp is LAUNCHED — v1.0.0, declared by Nic 2026-08-18.** Nic confirmed all necessary testing is done, closing Sessions 21–23 (alpha / beta / launch) without further rounds — the production deployment at greenqubes-ops.vercel.app IS the launched product. The webapp continues as the **desktop/office tool**; no new feature track is planned for it beyond the email-link sign-in below.

**Guided app tour (2026-09-04, LIVE on production — shipped for the Office Depot go-live).** Every team member's first sign-in on their role's home page offers a two-minute show-and-explain tour: the screen dims, `data-tour`-anchored spotlights walk the real UI one button at a time (6 role scripts: sales 14 steps, scheduler 12, coordinator 13, installer 10, designer 10, production 9), finishing on **Connect Telegram** then a "You're all set!" card. **Start tour → Choose your language** (English / 简体中文 / বাংলা) saves the person's app language through the existing `/api/user/lang` route — the tour's ONLY write; otherwise it is fully inert (full-screen click shield). Re-open anytime: profile picture → **App tour** (restarts skip the chooser). Engine in `src/features/tour/`: pure state machine + placement helper (standalone-tested), `TourProvider` mounted in CompanyBar, progress in sessionStorage (survives route hops incl. the role-less job-form CompanyBar), seen-flag `tour-seen:{userId}` per device, overlay portals to document.body at `z-[80]`. A validation suite enforces every script key in en+zh+**bn** — the tour is Nic's explicit scoped exception (2026-09-03) to the bn freeze; **both 中文 and বাংলা are unvetted** pending native-speaker corrections from the demo. Final review caught pre-ship: stale cross-role tour state could infinite-loop navigation; the dim dropped for up to 2s between steps; FCFS/Design anchors sat on data-dependent elements — all fixed. Smoke checklist `docs/guided-tour-smoke-test.md` + tickable artifact page (Nic previewed and merged without the full tick-through — his call). **When Workflow V3 merges, add Projects steps to the tour scripts.** Spec [superpowers/specs/2026-09-03-guided-tour-design.md](superpowers/specs/2026-09-03-guided-tour-design.md); plan [superpowers/plans/2026-09-03-guided-tour.md](superpowers/plans/2026-09-03-guided-tour.md).

**Workflow V3 — project containers (round 1 BUILT + smoke-cleared 2026-09-02 on `feat-workflow-v3`; NOT merged — Nic's rule: one clean-cut merge only when V3 is fully built).** A project is a container with labels, nothing copied: `job_projects` (migration **0051**, applied 2026-09-02) + `jobs.project_id`/`time_inherited`. Built in round 1: the **Multiple jobs** switch turning the New Job form into New Project; the **+ Add job** picker (keyword-seeded, pending-is-personal filtered); **new-job-in-project** with prefill, autosave-before-jump, an amber where-it-lands notice and an "(Untitled X)" title guard; **write-through timing inheritance** (a nested job follows the project's time until a user sets one — enforced on every write path incl. both clash modals, with a prev-state-aware `timingOnJobTimeEdit`); project **Files** (R2 `projects/` prefix, download route covered); **push-from-project** with per-job clash resolution via the job form's own ClashResolutionModal, atomic pending→scheduled transitions and ONE summary Telegram (`tplProjectPushed`); **/projects** list + read-only project view for designer/production; assistant folders renamed **Workspaces** (en/zh labels only). **Coordinator rule (Nic 2026-09-02): a pending job is shared between its sales person and the coordinators assigned on it via `job_coordinators` — coordinators see/push only those; scheduler+admin all.** Remaining before merge: **round 2** — schedule folding (green-ring folders, split punctuality stripe, date ranges, completed-in-day veil, All/Scheduled/Pending/Completed chips replacing the Pending+Completed tabs, pending-privacy RLS tightening, sort & filter dropdown) — and **round 3** — Notion-style month view + project pop-outs. Round-2 carry-overs from review: bucket INSERT/UPDATE RLS role predicates, zh literals on the new screens, picker tie-break newest-first, shared Telegram HTML-escape helper, duplicate-project row on a retried failed create. Spec [superpowers/specs/2026-09-02-workflow-v3-nesting-design.md](superpowers/specs/2026-09-02-workflow-v3-nesting-design.md); round-1 plan [superpowers/plans/2026-09-02-workflow-v3-round1-container-core.md](superpowers/plans/2026-09-02-workflow-v3-round1-container-core.md); feedback log [superpowers/specs/2026-09-02-workflow-v3-smoke-feedback.md](superpowers/specs/2026-09-02-workflow-v3-smoke-feedback.md); approved mockup `public/mockups/workflow-v3-nesting/`.

**Google first-login bounce fixed + the auth gatekeeper switched on (2026-08-31, live on production):** Nic's report — the first Google sign-in bounced to `/login?error=auth`, the second attempt worked, logout/login fine after. Root cause, reproduced locally and on production with a crafted stale cookie: a **stale session cookie** (an old login whose refresh key is dead) survived untouched to the OAuth callback; the callback's Supabase client tried to refresh it in the background while the PKCE code exchange ran; the failed refresh's clean-up (`_removeSession`) deleted the one-time **code-verifier** cookie mid-flight → `AuthPKCECodeVerifierMissingError` → `?error=auth` — and that same response finally cleared the stale cookie, so the retry worked. Underneath it: **`middleware.ts` at the repo root had never run on any deployment** — with the app under `src/`, Next.js only reads `src/middleware.ts` (build manifest empty; unknown pages 404'd instead of bouncing to login). Pages and routes guard themselves, so nobody noticed — but the middleware is the only place a server-side token refresh can be *saved* (Server Components can't write cookies), so refreshed sessions were lost and browsers were left holding dead cookies, which is how the stale cookie arose. Fixes (dev → preview → main same day): the callback drops any session cookie before creating its client and **logs the exact failure reason** (it used to swallow it — `[auth/callback]` in Vercel logs); the middleware moved to **`src/middleware.ts`**, skips `/api/*` (every route self-guards; crons, Telegram webhooks and the signed digest link carry their own secrets), `/ext/*`, `/auth/*` and `/mockups/*`, and copies Supabase's cookie updates onto every redirect (the documented `NextResponse.redirect` pitfall); the login page now shows the `?error=auth` / `account_removed` message (the red line existed but was never fed). Two new standalone suites (`src/lib/auth/session-cookies.test.ts` 9 checks, `middleware-scope.test.ts` 20 checks); all 11 suites green. Verified with curl probes on local, the dev preview (through the new bypass secret) and production: gatekeeper alive, stale cookie cleared on the first redirect, API/cron/webhook/ext/mockup paths untouched. **Vercel Protection Bypass for Automation** set up by Nic (dashboard only — the admin-page button was offered and declined); secret in `.env.local`, never in code. The Design Load branch inherits all of this when it lands on `dev` (only `en.ts` overlaps). Note: [fix/fix-auth-20260831-1-note.md](fix/fix-auth-20260831-1-note.md).

**Design Load — designer workflow V2.5 (BUILT 2026-08-26 → 28 on `feat-designer-load-flow`; LIVE on production since 2026-09-01 — clean-cut merge `feat-designer-load-flow` → `dev` → `main`, branch kept for history).** From Nic's "Workflow V2.5 Designers" diagram: sales/scheduler/coordinator assign **designers** to jobs (tile grid inside the new **Design brief** card — text + attachments, required on every edit-save of a scheduled job with a designer, pre-booking and schedulers exempt); an **AI scoring engine** (Haiku for text briefs, Sonnet when files are attached) scores complexity 1–5 + proposes a design due date (manual dates are sovereign; due dates **auto-shift with install-date moves** preserving the gap, with a keep-or-shift prompt when both change in one save); a **Design Load** bottom-nav tab shows every designer as a bottom-anchored stacked bar (colour = live urgency, height = deadline pressure, bubble beside the bar) with a designer-only **Board | My Jobs** toggle; designers tick **Design completed** through a required 1–5 rating slider (trust-checked against the AI's prediction + real turnaround; suspect ratings quarantined into an admin **Flagged strip**), sales/coordinator/scheduler can complete or **Reopen** without a rating; a daily cron sweeps unscored jobs and sends 3-day **Yes/No reminders** into the bell; an admin-only **AI Scores** tab shows accuracy charts + the scoring history. Rode along by Nic's decisions: **coordinators are now suggest-only for installers everywhere** (reverses part of Phase 2), sales-level job access for coordinators (create/push/delete/duplicate, PIC hand-off), `created_by` on jobs, production reads files (their photo uploads had been silently broken since V2), a **mobile hamburger drawer** replacing the bottom nav on phones, **draggable** floating buttons, notification-drawer upgrades (tap-to-read, assigner, X + Clear All, Telegram parity) and Chinese label/header fixes. Specs: [superpowers/specs/2026-08-18-design-load-design.md](superpowers/specs/2026-08-18-design-load-design.md) (+ Addendum) and the feedback log [superpowers/specs/2026-08-27-design-load-smoke-feedback.md](superpowers/specs/2026-08-27-design-load-smoke-feedback.md); plan [superpowers/plans/2026-08-18-designer-load-flow.md](superpowers/plans/2026-08-18-designer-load-flow.md). **Fix round 3 (2026-09-01 — B3 + edits 15–19):** B3 was never a broken send — the DB timeline showed the earlier-move test's only designer (Wan Jun) has no Telegram chat id, and the later move was excluded by the earlier-only rule; Telegram now fires on **every** due-date shift and every notification failure is logged (`[jobs/patch] …` in Vercel logs). Designers now hear about **every due-date event** — moved (auto-shift), install date moved with the due date kept, due date removed, due date set/changed by hand — four notification types (`design_due_shift` / `design_install_shift` / `design_due_removed` / `design_due_set`), bell card + Telegram each, no migration (`notifications.type` is text). The "cleared due date comes back" bug is fixed (the PATCH route echoed `updates.design_due_date ?? job.design_due_date` — `??` handed the old date back to the form). Floating buttons snap to the nearer screen edge on release and keep their side on resize (edit 15); override-modal copy (edit 16). Test data wiped 2026-09-01 via a dry-run-first one-off script (9 test jobs + all child rows + 10 R2 objects; 8 real client jobs kept). **Watch:** the first production `design_daily_cron` run (08:30 SGT) in Admin → Health. **Parked:** live whiteboard in the brief card (own session); seven small owner decisions listed in the checklist.

**Assistant upgrade — Phases 1 + 2 + 3 LIVE on production (all shipped 2026-08-25); spec approved 2026-08-24.** Four phases, each dev → preview → main. **Phase 1 SHIPPED (2026-08-25 am, feat-assistant):** `claude-sonnet-5` + adaptive thinking, `max_tokens` 8192, SDK `^0.120`, typed `web_search_20260209`, prompt-cached frozen system prefix (~1.4k tokens, must stay byte-identical), SSE `status`/`sources` events, Claude-grade UI (rAF streaming, Stop, source chips, two-row composer + mic dictation, sidebar, phone drawer — hamburger LEFT). **Phase 2 SHIPPED (2026-08-25 pm, feat-assistant-2):** the chat route is now an **agentic tool-use loop** (stream → execute tools → continue; cap 8 rounds, then tool_choice none + budget note; parallel calls concurrent; failures return `is_error`, never dropped) with **six read-only tools through the user-scoped client so RLS filters everything** — `search_knowledge` (replaces the pre-baked one-shot KB lookup; repeatable/rephrasable), `get_schedule`, `find_jobs`, `get_job` (no `job_financials` — money figures never reach the assistant), `get_team_workload`, `check_clashes` (name-resolved, reuses `timesOverlap`). Per-tool status lines in both chat surfaces (`statusLabels.ts`, unknown keys → "Thinking…"). **Memory:** migration 0046 (`asst_chats.summary` + `match_asst_chats` returns it — applied BEFORE code push); tagger (Haiku) now also produces a 2–3-sentence summary + `meaningful` boolean; only meaningful chats get summary + embedding (input = topic+summary; trivial chats save with both NULL so they never match); automatic past-chat recall feeds summaries (old rows fall back to first-msg truncation); **Memory manager** view (sidebar/drawer Brain button, z-[70] overlay) lists/edits (re-embeds)/forgets own memories via `/api/assistant/memory` (RLS-scoped). 3 new standalone test suites (tagger parse, summary fallback, tool schemas). Caveats: continuing a chat regenerates its summary (manual edit overwritten — YAGNI); **installer-login + two-account isolation checks deferred to PRODUCTION after all phases (Nic 2026-08-25)** — in nic-checklist. **Phase 3 SHIPPED (2026-08-25 evening, feat-assistant-3, no migration):** composer paperclip on the Assistant page (images jpg/png/webp/gif + PDF; ≤5 files / ≤20 MB per message, images ≤5 MB, PDFs ≤15 MB; HEIC rejected — the API can't read it) uploading to the R2 scratch prefix `asst-chat/{userId}/…` (never `files` rows; route guards keys to the caller's own prefix); the model reads them as base64 image/document blocks (expired scratch degrades to a text note); **`create_pending_job`** — the ONE action, sales/scheduler/coordinator/admin via `getEffectiveRole` (others get a polite in-chat refusal), system-prompt-enforced confirm-first, creates a pending job mirroring NewJobShell (4 default buckets) with attachments R2-copied into model-chosen buckets + a tappable link chip (`job_created` SSE, both chat surfaces); **Move-file-between-buckets** on the job form (PATCH `/api/files/[id]` — files has no UPDATE RLS; z-[60] picker; URL links move too); **30-day scratch cleanup cron** built same session (daily 03:00 SGT, events breadcrumb) — Nic's pick over deferring. Smoke passed on preview (mobile; unsupported-type rejection to re-check on PC — in nic-checklist); merged dev→main same day. **Phase 4 SHIPPED (2026-08-26, feat-assistant-4, migration 0047 db-pushed BEFORE code — Claude ran the push at Nic's request):** per-user **Projects** — `asst_projects` + `asst_project_files` (owner-only RLS) + `asst_chats.project_id` (ON DELETE SET NULL — deleting a project releases its chats, never deletes them); folders in the sidebar + phone drawer (create / settings / delete, nested chat rows, New-chat-in-project); ProjectPanel (name, instructions, reference files — **10 files / 20 MB per project, images+PDF**; Nic's caps call after 100 MB was ruled out by the API's 32 MB per-request ceiling); Move-to-project on every chat ⋮ menu; composer project chip. The chat route injects the project as a **cached prefix**: instructions as a second cached system block, files as blocks at the front of the first user message with a cache breakpoint, and (project chats only) the volatile date/user/recall context relocated to the last user message so the cached prefix stays byte-stable. `match_asst_chats` gained an optional `project_filter` (sibling chats recall first, general memory beneath — named-args backward compatible). New `REQUEST_FILE_BUDGET` (22 MB raw) guard degrades message attachments to text notes before the API cap can reject a request. Rode along: **Health-tab API usage window filter** (30 days / 7 days / Today — Today = SGT midnight). Same day on production: **deferred security checks ALL PASSED** (real installer login finds nothing on unassigned jobs and can't create; two accounts fully isolated incl. projects; unsupported-file rejection on PC), and a **false-confession guard** was added to SYSTEM_PREFIX after the installer test showed the assistant "apologising" for supposedly not verifying a prior answer (past-turn tool calls aren't persisted in chat history, so it must never speculate about them — shipped dev→main). **The assistant upgrade is COMPLETE — all four phases live on production.** Hard rules stand: memory strictly per-user (digest→vault stays the only cross-user bridge); job creation is the only action. Spec: [superpowers/specs/2026-08-24-assistant-upgrade-design.md](superpowers/specs/2026-08-24-assistant-upgrade-design.md); Phase 4 plan: [superpowers/plans/2026-08-26-assistant-upgrade-phase4.md](superpowers/plans/2026-08-26-assistant-upgrade-phase4.md). Next step: the mobile app roadmap (spec awaiting Nic's review).

**The mobile app is the new roadmap (spec approved 2026-08-18):** a native Android + iPhone app (React Native + Expo, one codebase in `mobile/`, same Supabase/R2/Vercel backend) built in 3 stages — 1: communication core (login incl. new **email sign-in link** on webapp + app, job list/detail, WhatsApp-grade per-job chat, push notifications, bell); 2: installer field work + offline; 3: schedule + create/assign + AI assistant. **Android ships first** (direct .apk, free); iPhone waits for the directors' greenlight on the Apple Developer subscription (US$99/yr). **Telegram bots retire only after Stage 3 + the iPhone rollout are both live.** Admin + FCFS board stay desktop-only; external installers keep their web links. Full spec: [superpowers/specs/2026-08-18-mobile-app-design.md](superpowers/specs/2026-08-18-mobile-app-design.md).

**Company rollout pack + Connect Telegram + digest pipeline fixes (2026-08-18, live on production):** the whole-company onboarding is prepared and the signup flow is self-service. **Connect Telegram** button in the account menu opens @GreenqubesOps_bot with a signed personal `/start` link (HMAC on the bot token — nothing stored in the DB, no migration); the ops webhook writes `telegram_chat_id` back and replies; any other DM to the bot returns instructions + the sender's chat ID (manual fallback). Rollout materials in `docs/rollout/` + published as artifacts: presentation deck (10 slides), 6 printable role cheat sheets, and Nic's runbook (email collection → pre-provision by email+role → sign in → two-tap Telegram link; digest subscribers must also press START on @Greenqubes_digest_bot — Telegram blocks bots from messaging people who never started them). **Digest pipeline fixed end-to-end the same evening:** (1) Admin → Digest tab sent via the OPS bot since Session 14 (predates the dedicated digest bot; votes on those messages were dead) → now routes via the digest bot; (2) votes failed with "account not registered" — Nic's soft-deleted old row shared his chat ID and the voter lookup `.single()`'d into two rows → digest voter/count/broadcast + D-Promote recipient queries now all filter `deleted_at` (Monday cron + timeout already did), ghost row's chat ID + digest tick cleared in data; (3) promoted notes wrote to the retired root `digest/` folder — the vault was reorganised under `Table of Content/` → auto-promote now targets **`Table of Content/Digest/`** with URL-encoded path segments; (4) **vault writes were dying silently: Vercel freezes a function once its response returns, killing fire-and-forget work** — `autoPromoteToVault` and the D-Promote send are now awaited (verified live: vote 20:24:09 → GitHub commit 20:24:17). Assistant now answers any message containing `D-Promote` with a canned wink (no model call; promotion behaviour unchanged). All test notes deleted from the vault after verification. Nic's Obsidian Git plugin auto-pulls every 11 min + on boot — promoted notes appear in Obsidian minutes after the vote; the assistant learns them at the 2:30 AM sync (instant-learn logged as a future checklist item).

**Revert completed jobs + bulk actions + list-card team lines (2026-08-24, live on production):** accidental completions are now undoable — every role except installer gets a **Revert to Scheduled** button (confirm popup) in the completed job's bottom bar, via a new server route `POST /api/jobs/[id]/revert-complete` (service client — RLS blocks sales/designer from editing completed rows; no migration). The revert clears `completed_at` and **restores the original `scheduled_at`** so the 0038 trigger can't push the job to the back of the FCFS queue; no Telegram fires (Nic's call — quiet undo). The scheduler's selection bar gains **Revert N** on the Completed tab and **Complete N** on the Schedule tab (same inline confirm as Delete). Schedule/pending/completed list cards show a team row — `Installer:` bottom-left (full names), left-aligned `Sales:`/`Coordinator:` box bottom-right, `NIL` when empty (sales POC name via follow-up users query, never embedded on jobs; installer views unchanged — their queries don't load team fields). Bell overdue cards gain the same Sales/Coordinator lines. The POC/coordinator Telegram header now reflects what happened: ✅ Installer Assigned / ❗ Installer Changed / ❌ Installer Removed (computed from the before/after assignee sets in the assign-installers route).

**Attachment delete fixed (2026-08-19, live on production):** deleting an attachment never worked — the `files` table has RLS enabled with **no DELETE policy** (deny-by-default since 0002), so the browser-side delete silently removed nothing and the file reappeared on every reload; R2 objects were never deleted at all. Deletes now go through server routes (`DELETE /api/files/[id]`, `DELETE /api/buckets/[id]`) that check role + job status (office roles only, installers never, completed jobs locked — rules in `src/lib/storage/job-file-permissions.ts`, 23 standalone tests), delete the **R2 object first**, then the DB row via the service client (no migration needed). Bucket delete now removes its files properly — before, `files.bucket_id ON DELETE SET NULL` orphaned them into **job chat** (chat = kind `attachment` with no bucket). UI only updates on server success and shows real error toasts. **Post-merge incident:** Nic deleted a bucket from a tab still running the pre-fix bundle — two files orphaned into job chat one last time; cleaned up via an approved one-off script (R2 + rows, verified 0 orphans remain). Lesson: refresh open tabs after a production deploy before testing. Reminder: R2 deletes propagate to the server-PC archive at the next 02:00 sync (mirror, not history).

**Workflow V2 is LIVE on production — clean-cut switchover completed 2026-08-03.** All four phases were built and smoke-tested on `feat-workflow-v2`, Nic's full regression test passed, and the branch was merged `feat-workflow-v2` → `dev` → `main` in one shot. **Session 19 pre-alpha testing PASSED clean 2026-08-17** (Nic's solo run on the wiped production slate — no issues found, so the Session 20 hotfix round is not needed).

**Accent rebrand to the company logo palette (2026-08-18, live on production):** all accent colors moved to the logo palette (Nic's call — accents only; neutrals + red untouched). Primary accent (buttons, own chat bubbles, active states) is now **moss green `#5A801F`** — the logo lime darkened so white text passes contrast; the **true logo lime `#91C740`** lives in a new `--lime` token reserved for small non-text highlights (chat live dot, favicon). Info/secondary = **logo slate `#6C747C`**; installer/success = **teal `#3E7F7B`** (Nic: the company green stays the only green in the app); warnings = **sand gold `#A9852F`**. Dark-mode soft tints regenerated. Token NAMES kept for compatibility (`--terracotta` etc. still appear in code — only the values changed; documented in CONTEXT.md). **Punctuality strict/flexible are company scheduling signal colors, NOT brand colors** (Nic caught this on the preview): new fixed tokens `--punct-strict` (#D14545) / `--punct-flex` (#3D6FB5) now drive the job-form toggle, schedule rows/legend/date-strip/month dots, FCFS bars/legend/assignment panel, and the external pages — never touch them in a future rebrand. Also 2026-08-18: **the top-bar logo is clickable → /schedule**, and /schedule now bounces installers to /installer (same pattern as /fcfs).

**Overdue bell alerts upgraded (2026-08-17, live on production):** the drawer's overdue cards now show project title (fallback "Untitled job"), company ("Untitled" when blank), day+date as `13/08/2026 (Thu)` (static English tables, no locale calls — the hydration lesson), and location. New **Mark as read** button in the drawer header greys all red alerts and clears the red bell + badge — remembered **per device per user** (localStorage `overdue-seen:{userId}`; a job rescheduled to a new overdue date re-alerts). **Alerts are team-scoped:** only the job's Person-in-Charge, coordinators and formally assigned installers see them (suggested installers never do); **scheduler + admin keep the company-wide view** (Nic's call 2026-08-17). Preview-as does not apply (UI role only — test scoping with a real login). Telegram overdue cron untouched. Also this session: Bryan's stale 2026-05-28 gitignore/settings commit **permanently skipped** via a `-s ours` merge — `.claude/settings.local.json` stays tracked; the session-start dev-bryan check is quiet again.

**Schedule list navigation redesigned (2026-08-05, live on production):** the endless date strip is gone — the list view now shows a windowed strip (week Mon–Sun ↔ full month, icon toggle remembered per device), paging arrows that peek without changing the selection, a persistent centred month label, a jump calendar off the heading, and an amber Today button. Filter chips (All/Today/This week/Upcoming) were **removed** — superseded by the strip + Today button; the List/Week/Month view toggle stays. **Weeks are Monday-start app-wide** (Week tab, Month tab, installer views, strip) and **date labels are always English** (`langToLocale` deleted per the CLAUDE.md hard rule). **No new Bengali translations** (boss decision 2026-08-03 via Nic): new i18n keys get en + zh only; bn falls back to English automatically.

**Job form reorganised (2026-08-06, live on production):** the New/Edit job pages share one responsive shell — **phone: 4 tabs** (Details / Team / Files / Chat; lossless switching, sticky action bar on every tab, opens on Details, chat Telegram links deep-link to the Chat tab via `?tab=chat`); **PC (lg+): two columns** (Details+Team left, Files+Chat right) with every card collapsible except Job Chat (chevron, per-device localStorage memory, read after mount only). New job uses the same shell with Files/Chat locked pre-save + a sticky action bar; production instructions unified into the Production card on both pages. The **Duplicate button** is live (was WIP): sales/scheduler/coordinator/admin, any status incl. completed → new pending job copying Details-tab fields (+" (Copy)" title), attachment buckets and production photos as **true R2 copies** (`/api/jobs/[id]/duplicate`); location blank, POC = duplicator, team/chat/tasks empty. **Bug fixed (was live on production):** bucket uploads share kind `attachment` with chat uploads and leaked into job chat — chat now excludes rows with a `bucket_id` on load + realtime.

- **Phase 1** (passed 2026-06-24): designer/coordinator/production roles live in DB + admin UI; approval workflow removed — sales pushes pending jobs directly to scheduled ("Push to Schedule"), all schedulers get a Telegram "New Job — Assign Installer" notification; /approvals redirects to /schedule.
- **Phase 2** (passed 2026-07-22): role-locked job form (designer view-only; production edits production-ready/DO/instructions/photos only; per-role action bars) and the **installer suggestion → assignment flow** — sales *suggests* (yellow, hidden from the installer until confirmed), coordinator/scheduler *formally assigns* (green, clears suggestions) and Telegram fires to the installer + sales POC + coordinators. Clash modal gained **Notify Scheduler** (holds job pending) / **Push Anyways**, plus a non-blocking amber heads-up for no-fixed-time "floater" installers. Preview-as now covers all 6 roles.

- **Phase 3** (passed 2026-07-22): the **FCFS Board** at `/fcfs`, built to the approved `fcfs-timeline-v2` mockup — day-view timeline (job rows ranked first-come-first-served, installer bars coloured by punctuality: blue flexible / red strict / bright-red hard clash / blue+amber-dashed soft warning / amber suggestion), AM/PM/AM-PM/9–6 zoom, clash chips + drawer, slide-in assignment panel (confirm/remove suggestions, add installers, Save & Notify via the Phase 2 assign-installers route). Clash-on-edit of a scheduled job closed (checkOnly + EditClashModal: scheduler gets Save Anyway/Go Back, coordinator gets Alert Scheduler & Save / Re-assign). **Rank counts from push-to-schedule** (`scheduled_at`, migration 0038 trigger) — Nic's call, since sales can't see each other's pending jobs. `/approvals` → `/fcfs`. **Day view only** — the mockup's Week / Month / By-Project / By-Installer toggles are deferred until designed.

**FCFS tab: NOT shown to installers** (Nic, 2026-07-22) — the board is a scheduler/coordinator planning tool; installers only need their own jobs. This overrides the Phase 1 "FCFS for every role" decision. It remains in the nav for scheduler / sales / coordinator / designer / production / admin; installers hitting `/fcfs` are bounced to `/installer`.

**⚠ Standing rule — overlays above the bottom nav** (Nic, 2026-07-22, in CLAUDE.md hard rules): every modal / drawer / bottom sheet / slide-in panel layers **above** BottomNav (`z-50`) — use `z-[60]`+. Nothing interactive may be hidden behind the nav.

- **Phase 4** (passed 2026-07-30): **external installer persistent links** — one lifetime token per outside person; public mobile page at `/ext/[token]` (accept/decline, upcoming/past, detail with attachments + interactive task list + person-in-charge call card — live chat for externals deferred). Managed from the **external installers bucket** in the Team card: every office role sees it, sales *suggests* externals (amber, invisible on the contact's page until a scheduler/coordinator confirms), managers add/copy-link/delete/restore — delete invalidates the link instantly, restore revives the **same token** with all history. **Sub-installer bucket** — same pool + suggestion rules as the main grid, excluded from clash detection (still counts in workload), own "Job Assigned — Supporting Role" Telegram. **Task list** — office builds it (drag-reorder, works on touch), installers/subs/externals tick with a progress bar. Copied links compose from the current site origin (preview vs production).

**Switchover verification (2026-08-03):** merged `dev` and `main` trees confirmed byte-identical to the regression-tested branch; type-check + production build green before each push; production confirmed live on V2 (`/ext` and `/fcfs` routes serving, old-code 404s gone).

**Branching strategy — clean cut EXECUTED (2026-08-03):** the clean-cut rule (decided 2026-06-24) is complete — V2 replaced the old workflow on dev/main in one shot. `feat-workflow-v2` is kept for historical record only (Nic) — do not push new changes to it. Normal deployment workflow resumes: all changes → `dev` first, Vercel preview, then `main`.

**Known bug (do not touch):** React hydration error #418 on `/schedule` in production — non-blocking, page works after refresh. Multiple fix attempts failed and were force-reverted. Leave it alone without a new specific hypothesis.

**File names + readable R2 folders (2026-08-06, live on production):** the original upload filename is stored in `files.name` (migration 0041) and shown in attachment buckets, job chat, production photos and the external installer page (pre-fix rows fall back to the old code-tail display); downloads save with the real name (`inline; filename*=UTF-8''` — Chinese-safe). Every job created from 2026-08-06 onward gets a readable R2 folder `{YYYY-MM-DD}_{Title-slug}_{8-char-code}` stamped by a DB trigger at creation (migration 0042) — frozen forever, old jobs keep `jobs/{jobId}/`, no existing object was renamed. **Deploy-order lesson:** run `npx supabase db push` BEFORE pushing code that SELECTs a new column — the preview crashed on every job click in the gap.

**⚠️ Backups — were never running until 2026-08-12.** The checklist had claimed since May that a nightly 02:00 backup was in place. It wasn't: rclone and the env var were configured, `backup.sh` was hand-tested once on 7 May, but **the Task Scheduler entry was never created**. `E:\Greenqubes-Archive\r2` held **zero files** — three months of attachments and photos existed only in Cloudflare R2. Now fixed: `Greenqubes Nightly Backup` (02:00) runs `scripts/nightly-backup.bat` → `backup.sh`; both it and `Greenqubes Obsidian Sync` (02:30) are `LogonType: Password` so they run **unattended**, not just when someone is logged in. Two bugs fixed on the way: `SUPABASE_DB_URL` pointed at the **IPv6-only** direct host (unreachable from this PC → switched to the IPv4 **session** pooler on **port 5432**; 6543 is transaction mode and cannot dump), and the stored password was stale (rotated). **Gotchas recorded:** a Supabase password reset takes 15–30s to reach the pooler and looks like a wrong password until it does; the Task Scheduler GUI silently reverts the unattended setting if its password prompt is cancelled; `schtasks /query` misreports `Logon Mode` — trust `(Get-ScheduledTask …).Principal.LogonType`. **Still unmonitored:** the backup writes no `events` row, so nothing would alert if it stopped again. See [infra/infra-backup-20260812-1-note.md](infra/infra-backup-20260812-1-note.md).

**Overdue alerts — scope + schedule (2026-08-06, live on production):** the overdue check now ignores any job dated more than **3 days** ago (`OVERDUE_LOOKBACK_DAYS` in `queries/notifications.ts`) — older jobs still sitting in `scheduled` are treated as abandoned data, not as work to chase. Cron runs **twice daily, 09:00 and 18:00 SGT** (`"0 1,10 * * *"` — Vercel schedules in UTC). Found by a manual production run that fired **27 alerts in one shot**: the query had a `date <= today` upper bound but no lower bound, so a backlog of stale `scheduled` jobs re-alerted on every run. The 2-hour dedupe window in `wasRecentlyNotified` is now redundant against the cron but still guards manual re-runs. **Test data note:** those 27 point at a real backlog of finished jobs never moved off `scheduled` — worth a cleanup pass before go-live.

**Live updates everywhere (2026-08-12, live on production):** the "must refresh to see changes" complaint was root-caused — the schedule + FCFS realtime listeners subscribed **without the user's JWT**, and RLS delivers `postgres_changes` events to authenticated listeners only, so they were silently deaf since 17.3 (the 2-min poll was papering over it; job chat had the same bug fixed 2026-05-19, chat-only). All realtime subscriptions now go through one shared hook **`src/lib/supabase/useLiveChannel.ts`** (getSession → `realtime.setAuth` → subscribe — the auth step can no longer be lost in a copy). **Migration 0043** added `job_assignees` + `job_tasks` to the realtime publication (they were never broadcast — FCFS's assignee listener had been waiting on events that never came). New live surfaces: **installer dashboard** (assigned jobs appear on their own; suggestions verified hidden in the live path with a real installer login) and the **job form** (hybrid rule: "clean syncs, dirty warns" — attachments/task ticks/team/status stream in; a jobs-row change while the form has unsaved edits shows an amber "This job was updated — tap to reload" banner instead of clobbering typing; own saves are echo-suppressed and re-baseline the form). Admin stays refresh-on-visit (Nic's call); assistant needs nothing. 2-min polls kept as fallback. `feat-live-updates` branch kept for **historical record** — no new pushes (Nic, 2026-08-12).

**Security audit + fixes (2026-08-13, live on production):** first full-app security review (access control/RLS, auth, all API routes, secrets, file storage, webhooks/crons, injection). Found **4 real access-control holes, all fixed and merged to main**. Headline (**CRITICAL**): the `users: own record update only` RLS policy (0002) had no column restriction, so any authenticated user could change their **own `role`** from the browser client — installer → admin/scheduler, exposing all jobs/financials/private assistant chats/audit log (admin became a pure DB role in 0019, so nothing else gated it). Fixed by **migration 0044** (BEFORE UPDATE trigger blocking non-admins from changing role/auth_id/email/deleted_at/digest_subscriber/telegram_chat_id on their own row). Also fixed: client tables + routes open to any authenticated user (delete/insert) → **migration 0045** locks writes to office roles (+ role gates on the 4 client routes); `/api/r2/download-url` signed any key with no object-level check → now authorizes by job visibility (bug screenshots scheduler/admin only, `files_r2_key_idx` in 0045); assistant `rename`/`updateChat` wrote to `asst_chats` via the service client with no ownership scope → now owner-scoped. Type-check + build green, preview-verified, `dev`→`main`. Stale "admin is email-gated" checklist note corrected. Lower-priority hardening (fail-closed webhook/cron secrets, notification-insert spoofing, Telegram HTML escaping) logged as non-blocking follow-ups. Full write-up: [security-audit-20260813.md](security-audit-20260813.md); session note [fix/fix-auth-20260813-1-note.md](fix/fix-auth-20260813-1-note.md).

**Test data wiped — production is a clean slate (2026-08-13):** every job (46 — including the ~27 stale finished-but-still-`scheduled` backlog), all their attachments/chat messages/buckets/tasks/assignments, the lone bug report, 27 crash logs, and every R2 object under `jobs/` + `bug-reports/` (44 objects, 15.6 MB) deleted from the production DB + bucket. **Kept:** users/logins, clients, external-contact pool (links stay alive, just show no jobs), assistant chats, KB, events, api_usage. Done via a one-off dry-run-first script — all counts verified zero afterwards; script deleted after use (Nic's call). The server-PC archive mirror clears itself of the deleted files at its next 02:00 sync (mirror, not history). **Same day: brand logo swap live on production** — `public/greenqubes-logo.png` replaces the text wordmark in the CompanyBar (every page) and the login card; Pre-Alpha tag removed (Nic's call); dark mode brightens the grey half via the `.brand-logo` CSS rule.

**DB migrations:** 0001–0053 all applied to the shared remote DB (prod/dev/v2 previews share it). **0053** lets installers SELECT all completed jobs + their files/chat/tasks/formal crew (read-only back-reference; `job_is_completed()` SECURITY DEFINER helper avoids jobs↔job_assignees policy recursion) — applied 2026-09-04. **0052** adds `users.subrole` / `is_driver` / `qualifications` (provisioning overhaul) — applied 2026-09-04. **0051** is Workflow V3 round 1 (`job_projects`, `jobs.project_id`/`time_inherited`, project-owned buckets/files + RLS) — applied 2026-09-02, additive and backward-compatible; the code that reads it lives only on `feat-workflow-v3` until the V3 merge. **0048–0050** are the Design Load set — `job_designers` + design columns + `design_scores`; designer file RLS + protected JO bucket; `created_by` trigger + coordinator/production file RLS — applied 2026-08-26/27, live with the 2026-09-01 merge. **0046** adds `asst_chats.summary` + summary in the `match_asst_chats` return (assistant memory, upgrade Phase 2). **0047** adds `asst_projects` + `asst_project_files` (owner-only RLS), `asst_chats.project_id` (ON DELETE SET NULL) and an optional `project_filter` param on `match_asst_chats` (drop/recreate — a signature change; named-arg callers unaffected, so it was deploy-order safe). **0044** guards privileged `users` columns against self-escalation (security fix — see above). **0045** locks `clients`/`client_contacts` insert+delete to office roles and adds `files_r2_key_idx`. 0033–0036 are the Workflow V2 Phase 1 set — incl. two hotfixes: 0035 (suggested_by FK dropped — second FK to users broke every `job_assignees(users(...))` embed with PGRST201) and 0036 (sales may transition own pending job to scheduled). **0037** is the Phase 2 migration: installer job-visibility RLS ignores suggestions (a suggested installer must not see the job), and coordinator + production gain `jobs` UPDATE. **0038** is the Phase 3 migration: `jobs.scheduled_at` + a trigger stamping every transition into `scheduled` — FCFS rank counts from push-to-schedule. **0041** adds `files.name` (original upload filename, display-only). **0042** adds `jobs.r2_folder` + a BEFORE INSERT trigger stamping the readable R2 folder at job creation. **0043** adds `job_assignees` + `job_tasks` to the `supabase_realtime` publication with `replica identity full` (live updates). **0039–0040** are the Phase 4 set: `external_contacts` (lifetime token — built-in `gen_random_uuid`, NOT `gen_random_bytes`: pgcrypto is not enabled on this DB), `job_external_contacts` (+ `is_suggestion`/`suggested_by` in 0040 — plain uuid, no users FK, the 0035 lesson), `job_tasks`, and `external_verification` on file_kind. Any new migration must stay backward-compatible with the code deployed on dev/main.

**⚠️ Standing rule — never embed `users` directly onto `jobs`** in a PostgREST select. `jobs` has several FKs to `users` (sales_poc_id, approved_by), and this has now broken twice: PGRST201 crashes (fixed by migration 0035) and the Phase 2 bug where `getInstallerJobs`' `sales_poc:users!jobs_sales_poc_id_fkey` embed made the installer's job list come back with empty `project_title`/`client`. Fetch the user in a follow-up query instead.

**Obsolete bugs removed:** "Save fails on approvals page" — approvals page no longer exists. AdminRoleModal double-Yes — confirmed not a bug (chore-config 2026-05-29). Friday bar in WeekWorkloadChart — fixed per checklist.

---

## Completed Sessions

| Session | Name | Key additions | Note |
|---|---|---|---|
| 1 | Scaffold | Next.js 15 + TS + Tailwind, design tokens, fonts, Supabase client, i18n stubs, feature folders | archived |
| 2 | DB Schema + RLS | 9 tables, enums, triggers, pgvector, RLS policies, seed data | archived |
| 3 | Auth | Google OAuth, login page, callback route, middleware, home page | archived |
| 4 | Shared Components | Card, Pill, Btn, Field, Input, Select, Toast, Modal, cn() | archived |
| 5 | Schedule (read-only) | List/week/month views, search, filter chips | archived |
| 6 | Job Detail | Full edit form, assignees, financials, file gallery, status transitions, live chat | archived |
| 7 | Approvals | Workload preview, scheduler approvals queue, approve/send-back, badge | archived |
| 8 | Notifications + Telegram | Bot utility, templates, approve/send-back API routes, overdue cron, bell drawer | archived |
| 9 | Installer Dashboard | Today/Up next/This week tabs, InstallerJobCard, role redirect | archived |
| 10 | Chat Thread + Voice Notes | Voice record/playback, back-nav, realtime approvals badge, submit API route | archived |
| 11 | AI Assistant | Streaming chat (Claude + web search + RAG), auto-tagger, pgvector retrieval | archived |
| 12 | Obsidian Sync + Monday Digest | Nightly vault sync, digest with Telegram majority voting, promote-to-Obsidian link | archived |
| 13–13.8 | Design Audit | Btn variants, colour tokens, page headers, installer Now card, WeekView/MonthView, BottomNav, UserMenu, New Job form | archived |
| 14 | Admin Page | Users tab, Digest tab, Health tab, API usage logging | archived |
| 15 | Crash Log | ErrorBoundary, /api/crash, crash_logs table, Admin Crashes tab | archived |
| 16 | R2 Helpers + Backup | Signed-URL upload helpers, CF Images binding, backup.sh rclone cold-archive | archived |
| 17 | Vercel Deploy | GitHub → Vercel, env vars, preview URL, Telegram webhook re-pointed | archived |
| 17.1 | Live Chat Fix | Simplified RLS on messages + files, favicon | archived |
| 17.2 | Calendar Nav + Live Schedule | UTC→local timezone fix, router.refresh() realtime, migration 0010, Vercel–GitHub wired | archived |
| 17.3 | Polling Fallback | Migration 0011 (REPLICA IDENTITY FULL), 2-min polling; hydration #418 fix attempts force-reverted | archived |
| 17.4 | Role Switcher | ai@greenqubes.com can preview as any role, cookie-based, amber chip | archived |
| 17.5 | Floating AI Chatbot | Persistent chat bubble on all pages, RAG + web search, sessionStorage handoff | archived |
| 17.6 | New Job Form + Schedule Filters | Pending tab (sales only), 15-min time picker, production instructions, chat handoff, pre-schedule locks, financials removed | archived |
| 17.7 | Required Fields + End Date | Custom TimeSelect, date_end + multi-day calendar, migration 0013, reactive header | archived |
| 17.8 | Installer Redesign | Completed tab, My Jobs redesign, list/week/month view toggle | archived |
| 17.9 | Bug Report Feature | Floating button, modal, R2 upload, bug_reports table (migration 0014), Telegram bug bot, Admin Bugs tab | archived |
| 17.10 | Nightly Bug Sync | sync-bugs.ts, nightly bat auto-commit + push | archived |
| 17.11 | Git Cleanup + Security | Hardcoded URI removed, DB password rotated, git history rebased 94→24 commits | archived |
| 18 | Full Design Review | 17 of 19 findings fixed, --bad/--bad-soft tokens added | archived |
| 18.1 | Design Edits | Company bar, notification bell, overdue styling, language switcher, Completed/Pending pages | archived |
| 18.2 | Design Edits | Pill casing, completed jobs hidden from Schedule tab, strict on-time legend colour | archived |
| 18.3 | Design Edits + Features | Label renames, Push for Approval flow, schedule filter, financials gate, Job Chat live indicator, whole-job lock, PendingFilesSection, Project Title (migration 0012) | archived |
| chore-docs | Docs Cleanup + Workflow Reset | Archived session notes, rewrote checklist, squashed plan/context, new session naming convention | [chore/chore-docs-20260509-1-note.md](chore/chore-docs-20260509-1-note.md) |
| chore-assistant | Permissions Audit + Assistant Codebase Review | Fewer-permission-prompts audit, deleted 11 orphaned .gitkeep files, traced chat thread + assistant full flow, identified 5 bugs/cleanup items for next session | [chore/chore-assistant-20260509-1-note.md](chore/chore-assistant-20260509-1-note.md) |
| feat-assistant | Assistant Fixes + History Sidebar Spec | Fixed duplicate asst_chats saves, streaming expand bug, added expand button to floating panel, deleted empty feature folders, tightened settings.json, designed + specced history sidebar | [feat/feat-assistant-20260511-1-note.md](feat/feat-assistant-20260511-1-note.md) |
| feat-assistant-2 | Assistant History Sidebar Implementation | Migrations 0015+0016, 3 new API routes, HistoryList + HistorySidebar components, mobile /assistant/history route, AssistantShell sidebar layout. Delete conversation button not working in preview — investigate next session | [feat/feat-assistant-20260511-2-note.md](feat/feat-assistant-20260511-2-note.md) |
| fix-assistant | Assistant Delete Button Fix + Delete Modal | Fixed mousedown/click race condition in HistoryList outside-click handler; replaced inline confirm with Delete Permanently modal; made deleteChat idempotent | [fix/fix-assistant-20260511-1-note.md](fix/fix-assistant-20260511-1-note.md) |
| feat-notifications | Telegram Templates + Obsidian Sync First Run | Finalised all Telegram notification templates (project title, POC fields, job assigned, bug report redesign); updated all 6 caller routes; wired Obsidian vault as git submodule; UI/UX Pro Max design system generated; pre-alpha testing done — bugs + features logged | [feat/feat-notifications-20260511-1-note.md](feat/feat-notifications-20260511-1-note.md) |
| feat-admin | Pre-Provision Users + Monday Digest Confirmed | Admin can now provision users by email without prior sign-in; migration 0017 (email column + partial unique index); auth callback links auth_id on first sign-in; UserRow shows "Waiting for sign-in" email; Monday digest confirmed working (skips correctly when no important conversations) | [feat/feat-admin-20260512-1-note.md](feat/feat-admin-20260512-1-note.md) |
| fix-prealphabugs | Pre-Alpha Hotfixes | Overdue cron wired in vercel.json; R2 CORS configured; bug report screenshot upload hardened; voice mic stream reused; attachment Telegram notifications; admin back arrow; role labels title-cased; time_end + description optional; form reset after save; NEXT_PUBLIC_APP_URL in Vercel | [fix/fix-bugs-20260513-1-note.md](fix/fix-bugs-20260513-1-note.md) |
| feat-admin-2 | Admin Role + Vercel Cron Fix | `admin` added to user_role enum; RLS policies updated; email gates replaced with role checks; AdminRoleModal in UsersTab; BottomNav admin tab; Vercel overdue cron changed to daily (was blocking Hobby plan deployments); migrations 0018–0020 | [feat/feat-admin-20260514-1-note.md](feat/feat-admin-20260514-1-note.md) |
| feat-jobs | AI Suggest Button | SuggestField component; /api/ai/suggest route (Haiku, SUGGEST_CONFIG); Project Title, Description, Notes, Production Instructions all wired; plain language rule added to CLAUDE.md | [feat/feat-jobs-20260514-1-note.md](feat/feat-jobs-20260514-1-note.md) |
| feat-design | Dark Mode | next-themes; ThemeProvider wrapper; .dark CSS token block (Claude Warm palette); UserMenu Moon/Sun toggle with localStorage + system preference detection; text-white→text-paper contrast fixes across 8 components | [feat/feat-design-20260514-1-note.md](feat/feat-design-20260514-1-note.md) |
| chore-jobs | Git + PR + Bulk Delete Design | Resolved rebase conflict (plan.md, nic-checklist.md, CONTEXT.md); PR opened dev→main; bulk delete feature designed (Design A: always-on checkboxes, bottom delete bar); spec + plan pending next session | [chore/chore-jobs-20260514-1-note.md](chore/chore-jobs-20260514-1-note.md) |
| feat-clash-resolution | Clash Detection + Resolution Modal + Workload Chart | Installer clash detection (proper time-overlap logic, no false positives for non-overlapping times); ClashResolutionModal (substitute selection with free/busy badges, keep-anyway, time shift via TimeSelect); travel-time warning for back-to-back jobs; team workload chart with week navigation (5-level bars, green→red, interactive installer panel); auto-save form before clash check; TimeSelect rolling from current time + HH:MM:SS normalisation; Delete Job button (sales, pending only); migration 0021 (years_experience, skills); major bug: save fails on approval page (approve & schedule); minor: Friday bar missing in chart | [feat/feat-clash-resolution-20260518-1-note.md](feat/feat-clash-resolution-20260518-1-note.md) |
| feat-chat | Chat Redesign + In-App Notifications | WhatsApp-style chat layout (own messages right/terracotta, others left with avatar+name); avatars show initials colour-coded by name; Supabase join key bug fixed (author/uploader→users); voice note live waveform; camera capture button; file auto-rename (voice/camera = `{username} {date} {time}`); in-app notifications for send-back events (migration 0022); bell drawer with mark-all-read + selective delete; sales POC shown on approval cards; grammar suggest button in send-back modal (Haiku); [Sent Back] messages wiped from job chat on approval | [feat/feat-chat-20260519-1-note.md](feat/feat-chat-20260519-1-note.md) |
| fix-chat | Job Chat Realtime Fix | Fixed realtime not delivering messages: createBrowserClient non-singleton → useMemo; admin missing from auth.uid() RLS policy → migration 0023; sales JWT not wired to realtime → explicit realtime.setAuth(); policies rewritten as EXISTS subqueries → migration 0024; avatar/name cache seeded from initialMessages + async fetch for new senders | [fix/fix-chat-20260519-1-note.md](fix/fix-chat-20260519-1-note.md) |
| feat-chat-2 | Chat Attachment Previews | Image files show inline thumbnail with terracotta footer on own messages; documents show compact card with coloured file-type icon (PDF/Word/Spreadsheet/ZIP) + download arrow; voice notes show play-button card with deterministic waveform bars that animate progress during playback (grey before play → terracotta as audio plays) | [feat/feat-chat-20260520-1-note.md](feat/feat-chat-20260520-1-note.md) |
| feat-digest | Dedicated Digest Bot + D-Promote + Voting Polish | Separate TELEGRAM_DIGEST_BOT_TOKEN + digest webhook; D-Promote secret command (force importance=5, immediate send to all digest_subscribers, word stripped from summary); strict >50% majority for both promote and dismiss; live poll count always shown on messages; buttons disabled for voter immediately after vote; 5-day timeout cron auto-resolves pending votes (strict majority yes → promoted, else dismissed); digest_subscriber flag controls all recipient queries; CLAUDE.md updated to ask about importance scoring categories each session | [feat/feat-digest-20260520-1-note.md](feat/feat-digest-20260520-1-note.md) |
| fix-assistant-history | Assistant History Bugs + New Chat Alignment | isDirtyRef prevents re-saving history-loaded chats as duplicates; existingId path updates existing row in place via updateChat() preserving original topic; activeChatIdRef for unmount path; refreshTrigger re-fetches sidebar after save; New Chat button pb-[72px] clears BottomNav overlap; polish item: sidebar refresh delay (optimistic update deferred) | [fix/fix-assistant-20260520-1-note.md](fix/fix-assistant-20260520-1-note.md) |
| feat-jobs | Job Form Redesign | Attachment buckets (upload, URL link, lightbox, rename, delete); SearchableSelect for company/POC/sales with add-new + confirm-delete; InstallerGrid 2-col toggle; ImageLightbox; production instructions always visible; AttachmentBuckets replaces AttachmentSection on edit page; NewJobShell rebuilt with installer grid + 3-button action bar; admin UsersTab: years_experience + skills chip fields for installer role; migrations 0025 (attachment_buckets + bucket_id/url_text on files) + 0026 (clients + client_contacts); feat-job-form-redesign branch → merged to main directly | [feat/feat-jobs-20260520-1-note.md](feat/feat-jobs-20260520-1-note.md) |
| feat-notifications-2 | Chat Notification Throttle | Throttled job chat Telegram notifications — at most once per 1 min per recipient; accurate unseen message count per person via job_chat_state table; new tplJobChatBatch template (count, project title, client, time, location, date); View in app → button opens system browser (InlineKeyboardButton url type); chat-read API route marks last_seen_at on chat open; ChatSection calls chat-read on mount; migration 0027 (job_chat_state); fixed ts column name bug (was created_at); CLAUDE.md branch exception removed — all to dev | [feat/feat-notifications-20260520-1-note.md](feat/feat-notifications-20260520-1-note.md) |
| chore-git-cleanup | Git Branch Cleanup | Merged dev into main (3 fix-assistant code patches + session notes); reset dev to match main; deleted feat-job-form-redesign branch locally and from remote; main, dev, and origin all in sync at same commit | [chore/chore-config-20260520-1-note.md](chore/chore-config-20260520-1-note.md) |
| ux-jobs | Job Form Action Bar Polish | InstallerGrid badge fix; SuggestField "Suggest" rename; upload API production_instructions fix; CoreSection + ProductionReadySection rewrite; NewJobShell Team card; AssigneeSection deleted; GreenqubesAI role dropdown locked; Person-in-Charge + Sub POC labels; X-button revert; sales pending action bar (Save Changes + Push for Approval); scheduler awaiting_approval bar (Send Back + Approve & Notify); Duplicate (WIP) placeholder; sales awaiting_approval: form locked + Recall button; sales scheduled: Push for Approval hidden + Save Changes full width | [ux/ux-jobs-20260521-1-note.md](ux/ux-jobs-20260521-1-note.md) |
| fix-jobs | Job Form + Schedule Fixes | Schedule: filter chips hidden in week (All only) / month (none) views; InstallerGrid: card highlights brand-green on select (no tick), readOnly prop, isInstallerDirty unlocks Save Changes (sales) + Save & notify (scheduler); back arrow → router.back() + "Back to Schedule"; installer locked for sales on scheduled jobs; AttachmentBuckets: fixed silent failures (url_link + production_instructions missing from file_kind DB enum — migration 0028), added success/error toasts, contentType fallback; new /api/jobs/[id]/notify-assigned route sends tplJobAssigned to newly added installers on scheduler save | [fix/fix-jobs-20260521-1-note.md](fix/fix-jobs-20260521-1-note.md) |
| ux-nav | Persistent Company Bar + Global Nav | CompanyBar shared component (GreenQubes + bell + user menu) sticky top-0 across all shells; NotificationDrawer refactored to fetch overdue jobs internally (no jobs prop); BottomNav kept on list/dashboard pages only; all 7 shells updated | [ux/ux-nav-20260521-1-note.md](ux/ux-nav-20260521-1-note.md) |
| feat-assistant-3 | Assistant Polish — Bulk Delete, Live Rename, Markdown, Layout | Per-user history isolation (migration 0030 drops cross-read RLS policy); optimistic "New Conversation" on first send; live auto-rename via Haiku after first response; bulk multi-select delete with confirmation modal; rename from ⋮ dropdown with persistence; message count + star importance hidden from UI; markdown rendering (MarkdownMessage — no new deps); allow typing while AI streams; full-width "← Assistant" sub-header above sidebar + content; New Chat button pb-[72px] clears BottomNav | [feat/feat-assistant-20260525-1-note.md](feat/feat-assistant-20260525-1-note.md) |
| chore-onboarding [Nic] | Bryan Onboarding Setup | docs-bryan/ folder created; assistant-onboarding.md, bryan-checklist.md, CLAUDE-bryan.md written and moved to docs-bryan/; dev-bryan branch created and pushed; CLAUDE.md updated — startup pull + dev-bryan merge/clash check + cross-checklist tagging; CLAUDE-bryan.md — branch rules, session end auto-commit/push, plan.md/context.md tagging, [Bryan] cross-checklist update | [chore/chore-config-20260525-1-note.md](chore/chore-config-20260525-1-note.md) |
| feat-vault [Nic] | Obsidian Vault Convention + Auto-Write | Vault folder scaffolding (clients, suppliers, sops, jobs, templates, contacts, digest) in greenqubes-kb; visibility + tagging convention spec; auto-write on Telegram digest promotion — majority vote → Sonnet generates note → GitHub API commits to vault/digest/; promote route replaced (copy-paste → auto-commit JSON); digest webhook fires auto-promote on majority; GITHUB_VAULT_REPO + GITHUB_VAULT_TOKEN env vars; nightly-obsidian-sync.bat (git pull + obsidian-sync); Task Scheduler setup guide | [feat/feat-vault-20260526-1-note.md](feat/feat-vault-20260526-1-note.md) |
| infra-config [Nic] | Task Scheduler — Nightly Obsidian Sync | Task Scheduler entry created on server PC (E drive) for nightly vault sync; trigger set to 2:30 AM daily; bat file tested and confirmed working; nightly-obsidian-sync.bat (git pull vault + obsidian-sync.ts) executes per schedule | [infra/infra-config-20260526-1-note.md](infra/infra-config-20260526-1-note.md) |
| fix-bugs | Bryan Migration Conflict + TS Build Fix | Renamed Bryan's 0015_bug_github_issue.sql → 0031 (0015 was taken); added github_issue_url to bug_reports Row type in types.ts; made it optional in Insert type and insertBugReport signature; DB already up to date (Bryan's agent had run db push) | [fix/fix-bugs-20260528-1-note.md](fix/fix-bugs-20260528-1-note.md) |
| feat-admin [Nic] | Admin Panel Improvements | Bugs tab forbidden fix (admin role); screenshot modal; three-bot health checks; obsidian sync + overdue cron event logging; Voyage/Telegram/R2 API usage logging; IP geolocation + non-SG anomaly rule; bug delete + multi-select + sort controls | [feat/feat-admin-20260528-1-note.md](feat/feat-admin-20260528-1-note.md) |
| feat-admin-3 [Nic] | Remove User / Revoke Access | Soft delete for active employees (deleted_at + Supabase Auth revocation); hard delete for provisioned users; migration 0032 (deleted_at column + partial index); removeUserAccess() with UserRemovalValidationError; DELETE /api/admin/users/[id]; auth callback blocks deleted re-login; middleware blocks deleted sessions; deleted_at IS NULL filters across all user queries; DeleteUserModal (two variants) + Remove button in UsersTab; pushed to dev preview; DB migration pending (npx supabase db push) | [feat/feat-admin-20260529-1-note.md](feat/feat-admin-20260529-1-note.md) |
| chore-config [Nic] | Checklist Cleanup | AdminRoleModal double-Yes confirmed not a bug (just load time); bulk delete jobs confirmed already fully implemented (checkboxes + delete bar + confirm step + parallel DELETE calls); checklist updated | [chore/chore-config-20260529-1-note.md](chore/chore-config-20260529-1-note.md) |
| fix-rag [Nic] | RAG + Knowledge Base Fixes | Voyage AI input_type fix (query/document); kb_chunks match threshold tuned to 0.35 for tabular price data; filename prepended to chunk embeddings for supplier name searchability; table rendering added to MarkdownMessage; supplier-template.md + DAMA.md + Jacky Printing pricelist added to vault; obsidian-sync workflow documented | [fix/fix-assistant-20260603-1-note.md](fix/fix-assistant-20260603-1-note.md) |
| infra-config [Nic] | Cron Schedule + R2 Folder Design | Overdue cron moved from 6pm SGT to 8am SGT (vercel.json); plan.md session note link discrepancy fixed; R2 human-readable folder pattern agreed: `{YYYY-MM-DD}_{Company}_{Client-Name}_{Project-Title}`; 4 sub-tasks noted in checklist for next session | [infra/infra-config-20260605-1-note.md](infra/infra-config-20260605-1-note.md) |
| chore-jobs [Nic] | Workflow V2 — Design + Plan | Full workflow redesign: 7 roles (+ designer, coordinator, production), approval workflow removed (sales pushes directly to scheduled), FCFS board replaces approvals tab, installer suggestion (yellow) vs formal assignment (green), external installer temp links (48hr), sub-installer relationship, task list bucket, external installer POC bucket (multi-team placeholder card). Branch `feat-workflow-v2` created. 25-task implementation plan written across 4 phases. No code written this session. | [chore/chore-jobs-20260605-1-note.md](chore/chore-jobs-20260605-1-note.md) |
| fix-schedule [Nic] | Vercel 404 Fix + Schedule Date Strip | Moved Workflow V2 mockups from `docs/superpowers/mockups/` → `public/mockups/workflow-v2/` so Vercel serves them as static assets (Next.js only serves `public/`). Schedule list view date strip now shows all dates in the full range (earliest job → latest job), not just dates with assigned jobs. `feat-workflow-v2` branch merged up to date with dev and pushed to remote — Vercel generates a separate preview for it. | [fix/fix-schedule-20260611-1-note.md](fix/fix-schedule-20260611-1-note.md) |
| feat-jobs [Nic] | Workflow V2 Phase 1 — Roles + Approval Removal | Migrations 0033–0036: designer/coordinator/production roles, installer-suggestion columns, RLS widening, suggested_by FK hotfix (PGRST201 had crashed /schedule on all deployments), sales pending→scheduled RLS fix. Approval workflow removed: approve/send-back routes + ApprovalCard/SendBackModal/ApprovalsShell deleted; "Push to Schedule" replaces "Push for Approval"; submit sets scheduled directly + tplNewJobCreated Telegram to all schedulers; /approvals → /schedule redirect; BottomNav FCFS tab for every role; new-job form success modal + failure surfacing. **Smoke test mid-way — sections 3–5 pending, see note** | [feat/feat-jobs-20260612-1-note.md](feat/feat-jobs-20260612-1-note.md) |
| fix-jobs [Nic] | Workflow V2 Phase 1 — Smoke Test Fixes (PASSED) | Finished Phase 1 smoke test (all 5 sections pass). Fixes: (1) New Job screen now runs the clash check before Push to Schedule (was pushing straight to /submit with no double-booking check); (2) clash modal — shifting the job time to a non-overlapping slot now auto-resolves the clash (reuses server overlap logic) + "Send to scheduler" renamed to V2's "Push to Schedule"; (3) chat attachment realtime now resolves uploader name (live photos showed "Unknown"/"?"); (4) installer My Jobs card now shows project_title (was client-only, so titled jobs looked blank). Clean-cut strategy agreed — no incremental merge to dev. Clash-on-edit-of-scheduled-job deferred to Phase 3. | [fix/fix-jobs-20260624-1-note.md](fix/fix-jobs-20260624-1-note.md) |
| feat-jobs [Nic] | Workflow V2 Phase 2 — Role-Locked Form + Installer Suggestion/Assignment (PASSED) | Tasks 9–15. Role-locked job form: designer view-only (no Save bar), production edits production-ready/DO/instructions/photos only, per-role action bars; Team card (POC/coordinators/notes) gated too. Installer **suggestion → assignment** flow: sales suggests (yellow, saves instantly via new `/suggest-installer`), coordinator/scheduler formally assigns (green) via new `/assign-installers` which clears suggestions and Telegrams the installer (`tplJobAssigned`) + sales POC/coordinators (`tplInstallerAssigned`). InstallerGrid rewritten as a 3-state presentation grid. **Migration 0037** — installer visibility RLS ignores suggestions; coordinator+production gain jobs UPDATE. Suggestions stripped from schedule/installer/overdue/workload/chat-recipient queries. Clash modal: **Notify Scheduler** (holds job pending, new `/notify-clash` + `tplClashNeedsReview`) + **Push Anyways**, and soft non-blocking heads-up for no-fixed-time floaters. Preview-as extended to all 6 roles. Bugs fixed: new-job form used real not effective role (suggestions saved as assignments); attachment buckets used auth id as `uploader_id` (FK violation — **production bug too**); installer list blank title (jobs→users embed). FCFS dropped from installer nav. | [feat/feat-jobs-20260722-1-note.md](feat/feat-jobs-20260722-1-note.md) |
| feat-jobs [Nic] | Workflow V2 Phase 3 — FCFS Board (PASSED) | Tasks 16–19 + smoke-test fixes. FCFS day-timeline board at `/fcfs` per the approved mockup: job rows ranked FCFS, punctuality-coloured installer bars, AM/PM/9–6 zoom, clash chips + drawer, slide-in assignment panel (confirm/remove suggestions, add, Save & Notify). Clash-on-edit of scheduled jobs closed (checkOnly + EditClashModal — scheduler: Save Anyway/Go Back; coordinator: Alert Scheduler & Save). **Migration 0038**: `scheduled_at` trigger — rank counts from push-to-schedule (sales can't see each other's pending jobs). Smoke-test fixes: wide-view bar alignment (flex hour columns), **overlays above BottomNav** (new hard rule in CLAUDE.md, z-[60]), scrollable clash chips, bolder priority labels, "All day" on schedule list + board, Person-in-Charge/Sub-POC dropdowns now list **all office roles** (sales-only / sales-scheduler-admin filters hid newly provisioned coordinators/designers/production). `/approvals`→`/fcfs`. Day view only — other view toggles deferred. | [feat/feat-jobs-20260722-2-note.md](feat/feat-jobs-20260722-2-note.md) |
| feat-jobs [Nic] | Workflow V2 Phase 4 — External Links + Sub-installers + Task List (PASSED) | Tasks 20–25 + smoke-test feedback. Migrations 0039 + 0040. Public `/ext/[token]` page (accept/decline, detail, task ticking — chat deferred); external POC bucket for every office role (sales suggest → manager confirm, delete/restore with same-token revival); sub-installer bucket + "Supporting Role" Telegram; task list with touch drag-reorder. Fixes: copied links pointed at production (now current origin); sub Telegram silently rejected (unclosed bold tag); 0039 pgcrypto dependency removed; `jobs` Row types gained `project_title`/`date_end`. **V2 build complete — regression test + clean-cut switchover next.** | [feat/feat-jobs-20260730-1-note.md](feat/feat-jobs-20260730-1-note.md) |
| chore-config [Nic] | Workflow V2 Clean-Cut Switchover | Full V2 regression test passed (Nic, on the preview) → `feat-workflow-v2` merged into `dev` (3 doc-file conflicts resolved to the newer branch versions) → `dev` merged into `main`; both pushed. Merged trees verified byte-identical to the tested branch; type-check + production build green; production verified live on V2 (`/ext` + `/fcfs` serving). `feat-workflow-v2` kept for historical record — no new pushes to it. Normal dev-first deploy workflow resumes. | [chore/chore-config-20260803-1-note.md](chore/chore-config-20260803-1-note.md) |
| ux-schedule [Nic] | Schedule List Scrolling UX | Windowed date strip (week Mon–Sun ↔ month, icon toggle persisted per device), peek paging arrows, persistent centred month label, jump calendar from the heading, amber Today button (left side); filter chips removed; Monday-start weeks app-wide (Week/Month tabs + installer views); date labels always English (`langToLocale` deleted); `scrollbar-none` utility finally defined; page spacing + legend tightened; interactive mockup at `public/mockups/schedule-list-ux/`; smoke test passed desktop + mobile (2 feedback rounds); merged dev→main, live on production. Bryan's obsidian-sync frontmatter tidy-up rode along. | [ux/ux-schedule-20260805-1-note.md](ux/ux-schedule-20260805-1-note.md) |
| ux-jobs [Nic] | Job Form Tabs + Duplicate + Chat Leak Fix | New/Edit job pages share one responsive shell: phone = 4 tabs (Details/Team/Files/Chat, all groups stay mounted so switching never loses typing, sticky action bar everywhere, `?tab=chat` deep link wired to chat Telegram notifications); PC = two columns (Details+Team \| Files+Chat) with collapsible cards except chat (`CollapseCard` + `useCardCollapse`, per-device localStorage, hydration-safe); New job: same shell, Files/Chat locked pre-save, sticky bar, production instructions moved to Production card (matches Edit). Duplicate button live: `/api/jobs/[id]/duplicate` copies Details fields (+" (Copy)" title), buckets + production photos as true R2 copies (`copyObject`); location blank, POC = duplicator, team/chat/tasks fresh, status pending. Fix (pre-existing prod bug): bucket uploads (kind `attachment` + `bucket_id`) leaked into job chat on load + realtime. Merged dev→main same day. Future notes: Telegram notification tracker, sub-jobs under a parent job. | [ux/ux-jobs-20260806-1-note.md](ux/ux-jobs-20260806-1-note.md) |
| feat-files [Nic] | File Names + Readable R2 Folders | Migration 0041 (`files.name`): original upload filename saved at all 5 upload paths and shown in buckets, job chat (incl. realtime), production photos, external installer page — fallback to key-tail for pre-fix rows; camera captures keep their generated `{username} {date} {time}` name; downloads carry the real name via `inline; filename*=UTF-8''` (Chinese-safe). Migration 0042 (`jobs.r2_folder` + BEFORE INSERT trigger, 0038 pattern): NEW jobs upload into readable R2 folders `{YYYY-MM-DD}_{Title-slug}_{8-char-code}` — frozen at creation, Unicode titles kept, old jobs stay `jobs/{jobId}/`, nothing renamed. Duplicate copies names into the copy's own folder; upload-url route now 404s jobs the caller can't see; dead `AttachmentSection.tsx` + `insertFile()` deleted. Supersedes the 2026-06-05 folder plan. **Incident:** preview crashed on job click — code selecting `files.name` deployed before the migration ran (lesson: db push BEFORE code push); parallel-session collision on `dev` resolved by working in an isolated worktree. Smoke test passed; merged dev→main same day. | [feat/feat-files-20260806-1-note.md](feat/feat-files-20260806-1-note.md) |
| infra-notifications [Nic] | Overdue Alert Scope + Schedule | Overdue check given a 3-day lower bound (`OVERDUE_LOOKBACK_DAYS`) — jobs older than that are treated as abandoned, not overdue. Cron moved from daily 8am → every 2 hours → settled on **twice daily, 9am + 6pm SGT** (`"0 1,10 * * *"`). Diagnosed by a manual production run that sent **27 real Telegram alerts** at once: `getOverdueJobs` bounded `date` on the top end only, so every stale `scheduled` job re-fired forever. Also corrected an earlier misread — the `tplJobOverdue` bold tag was never broken. Backlog of ~27 finished-but-still-`scheduled` jobs logged for a pre-go-live cleanup. Merged dev→main same day. | [infra/infra-notifications-20260806-1-note.md](infra/infra-notifications-20260806-1-note.md) |
| infra-config [Nic] | Obsidian Sync Outage + KB Restore | Vault sync silent since 7 Jun (57 days) — `events` log pinned the stop to the 2:30 AM run; cause: print-server hang → restart → signed-out PC, tasks were "run only when logged on" (permanent fix in same-day infra-backup session). `kb_chunks` had shrunk to 3 rows — supplier pricelists deleted as "stale" by runs from an outdated vault clone. Two script bugs fixed with standalone tests: frontmatter parser misread Obsidian's multi-line YAML (garbage visibility tokens; supplier costing could have leaked or vanished — 13 tests) and chunker split CRLF checkouts into one giant chunk + never trimmed orphaned rows (7 tests); both extracted to `scripts/lib/`, both on `main` (server pulls main). PTW files locked to sales/scheduler/coordinator (Nic's call). KB verified restored: 15 chunks, correct tags, 5+ consecutive unattended nightly runs. `docs/server-pc-sync-checklist.md` added. Parallel-session collision lesson: verify branch, use worktrees. | [infra/infra-config-20260812-1-note.md](infra/infra-config-20260812-1-note.md) |
| infra-backup [Nic] | Nightly Backup Was Never Running | Discovered the 02:00 backup had **never been scheduled** despite the checklist claiming otherwise since May — `E:\Greenqubes-Archive\r2` held 0 files, DB dumps stopped 7 May. Verified by scanning all 380 scheduled tasks for `.sh`/`bash`/`rclone` (zero hits). Created `scripts/nightly-backup.bat` + task `Greenqubes Nightly Backup` (02:00). Fixed two blockers: `SUPABASE_DB_URL` pointed at the IPv6-only direct host (unreachable → IPv4 session pooler, port 5432 not 6543), and the password was stale (rotated; reset takes 15–30s to propagate and reads as "wrong password" until it does). Both backup and Obsidian Sync moved to `LogonType: Password` so they run unattended — via `Set-ScheduledTask`, since the GUI silently reverts if its password prompt is cancelled, and `schtasks /query` misreports `Logon Mode`. Verified: 41 R2 files / 15.3 MiB, 205 KB dump with 22 tables + data and a clean completion marker. Open: old password may live in Vercel/Bryan's env; R2 sync is a mirror not history; backup has no health-tab monitoring. | [infra/infra-backup-20260812-1-note.md](infra/infra-backup-20260812-1-note.md) |
| feat-realtime [Nic] | Live Updates Everywhere | Root cause of the long-standing "must refresh" complaint: schedule + FCFS realtime listeners subscribed without the user's JWT — RLS delivers events to authenticated listeners only, so both were silently deaf (job chat had the identical bug, fixed 2026-05-19 chat-only; 17.3's 2-min poll was the crutch). New shared hook `useLiveChannel` (setAuth → subscribe → cleanup in one place); ScheduleShell + FCFSShell refactored onto it. Migration 0043: `job_assignees` + `job_tasks` into the realtime publication (never broadcast before). Installer dashboard live (suggestions verified hidden with a real installer login on production). Job form hybrid: files/tasks/team/status stream in; jobs-row change vs unsaved edits → amber tap-to-reload banner; own-save echo suppression + clean re-baseline; `refreshKey` re-pull on AttachmentBuckets/TaskListSection. Admin left refresh-on-visit, assistant untouched (by design). Built on `feat-live-updates` off main (kept for archive, no new pushes); other agent's feat-files work merged through; migration renumbered 0041→0043 mid-flight. | [feat/feat-realtime-20260812-1-note.md](feat/feat-realtime-20260812-1-note.md) |
| fix-auth [Nic] | Security Audit + Fixes | First full-app security review (access control/RLS, auth, all API routes, secrets, file storage, webhooks/crons, injection). **4 real access-control findings, all fixed live on production.** #1 CRITICAL — the `users: own record update only` RLS (0002) had no column restriction, so any authenticated user could change their own `role` from the browser (installer → admin/scheduler = all jobs/financials/private chats/audit log; admin became a pure DB role in 0019). Fixed by **migration 0044** (BEFORE UPDATE trigger guarding role/auth_id/email/deleted_at/digest_subscriber/telegram_chat_id; service-role + real-admin paths unaffected). #2 MEDIUM — `clients`/`client_contacts` `using(true)` insert+delete → **migration 0045** locks writes to office roles + 4 route role-gates. #3 MEDIUM — `/api/r2/download-url` signed any key with no object check → now authorizes by job visibility (bug screenshots scheduler/admin only; `files_r2_key_idx`). #4 LOW — assistant `rename`/`updateChat` wrote asst_chats via service client unscoped → owner-scoped. Type-check + build green; preview-verified; migrations 0044+0045 applied; `dev`→`main`. Stale "admin is email-gated" note corrected. Hardening (fail-closed webhook/cron secrets, notification-insert spoofing, Telegram HTML escaping) logged non-blocking. Audit: [security-audit-20260813.md](security-audit-20260813.md). | [fix/fix-auth-20260813-1-note.md](fix/fix-auth-20260813-1-note.md) |
| chore-db [Nic] | Brand Logo + Test-Data Wipe | Logo PNG replaces text wordmark in CompanyBar + login (Pre-Alpha tag removed, dark-mode `.brand-logo` brightness lift; dev→main same day). Production test-data wipe via one-off dry-run-first script: 46 jobs + all cascaded children (files, chats, buckets, tasks, assignments, external links), 1 bug report, 27 crash logs, 44 R2 objects (15.6 MB, `jobs/` + `bug-reports/`) deleted; users/clients/external pool/asst chats/KB kept; all counts verified zero; script deleted after use. | [chore/chore-db-20260813-1-note.md](chore/chore-db-20260813-1-note.md) |
| ux-notifications [Nic] | Overdue Bell Alerts — Rich Cards + Mark-as-Read + Team Scoping | Session 19 pre-alpha PASSED clean (no issues; Session 20 skipped — green light for alpha). Overdue drawer cards show project title / company ("Untitled" when blank) / `13/08/2026 (Thu)` day+date / location. Mark as read button greys red alerts + clears red bell, per device per user (localStorage; reschedule to a new overdue date re-alerts). Alerts team-scoped: POC + coordinators + formally assigned installers only (suggestions never alert); scheduler + admin see all. dev→main same day. Bryan's stale settings/gitignore commit permanently skipped (`-s ours`). | [ux/ux-notifications-20260817-1-note.md](ux/ux-notifications-20260817-1-note.md) |
| visual-design [Nic] | Logo Palette Rebrand + Clickable Logo | Accents remapped to the company logo palette (accents only — neutrals/red untouched): moss green #5A801F primary (logo lime darkened for contrast), new `--lime` #91C740 highlight token (live dot, favicon), slate #6C747C info, teal #3E7F7B installer/success (company green stays unique), sand gold #A9852F warning; dark softs regenerated; token names kept. Punctuality extracted to fixed signal tokens `--punct-strict` #D14545 / `--punct-flex` #3D6FB5 (company scheduling indicators — never rebrand) across toggle/schedule/FCFS/external. Top-bar logo clickable → /schedule; installers hitting /schedule bounced to /installer. dev→main same day (two merges). | [visual/visual-design-20260818-1-note.md](visual/visual-design-20260818-1-note.md) |
| chore-mobile [Nic] | Webapp Launched v1.0.0 + Mobile App Design Spec | Nic declared the webapp LAUNCHED at v1.0.0 (all testing done — Sessions 21–23 closed without further rounds; webapp continues as the desktop/office tool). Mobile app (Android + iPhone) designed + specced in a brainstorming session: React Native + Expo, one codebase in `mobile/`, same backend; 3 stages (communication core → installer field work + offline → office on the go + AI); Android-first via direct .apk, iPhone gated on the directors' Apple Developer greenlight (US$99/yr, org enrollment, D-U-N-S first); Telegram retires only after Stage 3 + iPhone rollout both live; per-job chat is the make-or-break feature; email magic-link sign-in approved for webapp + app. Spec committed: [superpowers/specs/2026-08-18-mobile-app-design.md](superpowers/specs/2026-08-18-mobile-app-design.md). No code written. | [chore/chore-mobile-20260818-1-note.md](chore/chore-mobile-20260818-1-note.md) |
| feat-rollout [Nic] | Company Rollout Pack + Connect Telegram + Digest Pipeline Fixes | Rollout materials (docs/rollout/ + artifacts): 10-slide deck, 6 printable role cheat sheets, Nic's runbook (collect exact Google emails → pre-provision email+name+role → sign in links automatically → Connect Telegram two taps; digest subscribers also press START on @Greenqubes_digest_bot). **Connect Telegram self-link**: signed /start deep link (HMAC on bot token, no DB change, 15 tests), webhook writes chat ID + replies; other DMs get instructions + chat ID. Digest fixes, all live: Admin Digest tab routed via ops bot since Session 14 → digest bot; deleted-user rows broke voting ("not registered" — `.single()` hit Nic's ghost row sharing his chat ID) + inflated the majority denominator → `deleted_at` filters on voter/count/broadcast/D-Promote queries + ghost row data cleared; promoted notes → `Table of Content/Digest/` (vault reorganised; URL-encoded path); **fire-and-forget vault write killed by Vercel's response freeze → awaited** (verified: vote→GitHub commit in 8s). Assistant winks at `D-Promote`. Test notes cleaned from vault. | [feat/feat-rollout-20260818-1-note.md](feat/feat-rollout-20260818-1-note.md) |

| fix-files [Nic] | Attachment Delete Fix | Root cause of "deleted attachments come back": `files` has RLS but **no DELETE policy** (deny-by-default since 0002) — browser deletes silently removed 0 rows and the UI faked success; R2 objects never deleted. New server routes `DELETE /api/files/[id]` + `DELETE /api/buckets/[id]`: role+status check via `canManageJobFiles` (office roles only, installers never, completed locked — `job-file-permissions.ts`, 23 standalone tests), R2 object deleted first, then DB row (service client, no migration). Bucket delete now removes its files too (was orphaning them into job chat via `bucket_id ON DELETE SET NULL`). AttachmentBuckets state updates only on server success + real error toasts (rename too). Verified on preview by Nic; dev→main same day. Post-merge: a stale tab (pre-fix bundle) orphaned 2 files into chat one last time — cleaned up via approved one-off script, 0 orphans remain. | [fix/fix-files-20260819-1-note.md](fix/fix-files-20260819-1-note.md) |

| feat-schedule [Nic] | Revert Completed Jobs + Bulk Actions + Card Team Lines | Revert to Scheduled button on completed job pages (all roles except installer, confirm modal, new `revert-complete` server route — RLS blocks sales/designer client-side; `completed_at` cleared, `scheduled_at` restored so FCFS rank survives; no Telegram, Nic's call). Scheduler selection bar: Revert N (Completed tab) + Complete N (Schedule tab) with inline confirm. List cards (schedule/pending/completed): `Installer:` bottom-left, left-aligned `Sales:`/`Coordinator:` box bottom-right, NIL when empty (POC name via follow-up query; installer views untouched). Bell overdue cards: Sales + Coordinator lines. Telegram POC/coordinator header now ✅ Assigned / ❗ Changed / ❌ Removed from before/after assignee sets. Merged dev→main same day. | [feat/feat-schedule-20260824-1-note.md](feat/feat-schedule-20260824-1-note.md) |

| chore-assistant [Nic] | Assistant Upgrade Design Spec | Brainstormed + specced the full assistant overhaul (no code): Sonnet 5 + thinking, Claude-grade UI/UX (sidebar, composer + mic dictation, mobile drawer, streaming smoothness), read-only live-data tools + agentic KB search, per-user memory with only-meaningful gate + Memory manager, attachments → pending job flow with bucket auto-filing + Move-to-bucket, Projects (folders/files/instructions/linked memory). Privacy hard rules recorded (per-user memory; vault = only cross-user bridge). Vault submodule pointer also updated. Spec: [superpowers/specs/2026-08-24-assistant-upgrade-design.md](superpowers/specs/2026-08-24-assistant-upgrade-design.md) | [chore/chore-assistant-20260824-1-note.md](chore/chore-assistant-20260824-1-note.md) |

| feat-assistant [Nic] | Assistant Upgrade Phase 1 — Sonnet 5 Brain + Claude-Grade UI (SHIPPED) | Chat route: `claude-sonnet-5`, adaptive thinking, `max_tokens` 8192, SDK `^0.39`→`^0.120`, typed `web_search_20260209`, prompt-cached frozen system prefix (persona + guidelines + app glossary; volatile date/user/KB context after the breakpoint), SSE `status`/`sources` events, abort handling, Sonnet 5 + cache-rate + per-search cost logging. UI: rAF-buffered smooth streaming, sticky-bottom-only scroll + resume button, status lines ("Thinking…"/"Searching the web…"), Stop button (partial kept + saveable), web source chips wired at last, two-row composer (+ attach placeholder for Phase 3, Web Speech mic dictation feature-detected, send/stop, no model picker), sidebar New-chat-on-top + skeleton rows + Pin icon (📌 emoji removed), phone chat-first layout: hamburger LEFT → left drawer at z-[60] (branding, New chat, chats), Home icon right → schedule, CompanyBar + sub-header shed, `/assistant/history` → redirect (MobileHistoryShell deleted), Fraunces first-name greeting. Feedback round from Nic's phone test folded in same day. HealthTab flat-rate Anthropic drift check removed (cache discounts + mixed models + search fees). Smoke test passed → merged dev→main same day. | [feat/feat-assistant-20260825-1-note.md](feat/feat-assistant-20260825-1-note.md) |

| feat-assistant-2 [Nic] | Assistant Upgrade Phase 2 — Live-Data Tools + Agentic KB + Memory (SHIPPED) | Chat route rebuilt as an agentic tool-use loop (cap 8, parallel execution, is_error results, final round forces an answer); six read-only tools on the **user-scoped client** (RLS = the privacy mechanism): search_knowledge / get_schedule / find_jobs / get_job (financials excluded) / get_team_workload / check_clashes; pre-baked KB retrieval removed, automatic past-chat recall stays. Migration 0046 (`asst_chats.summary`, `match_asst_chats` returns it) db-pushed by Nic BEFORE any code push. Tagger emits summary + meaningful gate (D-Promote forces importance only); saveChat/updateChat embed topic+summary for meaningful chats, NULL otherwise; Memory manager view (Brain button, z-[70]) with edit-re-embed/forget via RLS-scoped `/api/assistant/memory`. Per-tool status lines both surfaces + en/zh strings. 3 new standalone test suites (103 checks total green). Found: this clone's node_modules still had SDK 0.39 (npm install never run after the Phase 1 pull). Nic deferred installer-login + two-account isolation checks to production after all phases. Preview smoke passed → merged dev→main same day. | [feat/feat-assistant-20260825-2-note.md](feat/feat-assistant-20260825-2-note.md) |

| feat-assistant-3 [Nic] | Assistant Upgrade Phase 3 — Chat Attachments → Pending Job (SHIPPED) | Composer paperclip (Assistant page only): images + PDF to R2 scratch `asst-chat/{userId}/…` via new `/api/assistant/upload-url` (caps 5/20 MB per message, HEIC rejected — API limit); chat route turns attachments into base64 image/document blocks (own-prefix key guard, expired-scratch text fallback); `create_pending_job` tool — role-gated (`getEffectiveRole`), confirm-first (system prompt), pending-only insert on the USER-SCOPED client mirroring NewJobShell + 4 default buckets, attachments R2-copied into model-chosen buckets, `job_created` SSE → tappable chip in both chat surfaces; "Creating the job…" status line; Move-file-between-buckets (PATCH `/api/files/[id]`, z-[60] `MoveFileModal`, URL links too); 30-day scratch-cleanup cron (daily 03:00 SGT) built instead of deferred (Nic). New standalone suite (attachments — 25 checks); all 8 repo suites green; no migration. Smoke passed → merged dev→main same day. Future items logged: filing-only Office attachments; Office-file text extraction (needs dependency OK). | [feat/feat-assistant-20260825-3-note.md](feat/feat-assistant-20260825-3-note.md) |

| feat-assistant-4 [Nic] | Assistant Upgrade Phase 4 — Projects (SHIPPED — upgrade COMPLETE) | Migration 0047 (`asst_projects` + `asst_project_files` owner-RLS, `asst_chats.project_id` SET NULL, `match_asst_chats` `project_filter`) db-pushed BEFORE code — Claude ran the push at Nic's request. Project folders in sidebar + drawer (create/settings/delete — chats released, never deleted; nested rows reuse an exported `ChatRow`), ProjectPanel (name / instructions / files — 10 files / 20 MB, images+PDF; Nic's pick, 100 MB impossible under the 32 MB API request cap), Move-to-project picker, composer chip, new-chat-in-project. Chat route: instructions as a 2nd cached system block, files cached at the front of the first user message, volatile context relocated to the last user message (project chats only — keeps the cached prefix byte-stable); recall scoped siblings-first. `REQUEST_FILE_BUDGET` (22 MB) guard degrades attachments to notes before the API cap. Health tab: usage window filter (30d / 7d / Today — SGT midnight). 2 new standalone suites; all 9 green. Same day: deferred production security checks ALL PASSED (installer isolation, two accounts, file rejection) + false-confession guard added to SYSTEM_PREFIX (past-turn tool calls aren't persisted — never speculate about them) → dev→main. | [feat/feat-assistant-20260826-1-note.md](feat/feat-assistant-20260826-1-note.md) |
| feat-design [Nic] | Design Load — Designer Workflow V2.5 (BUILT on feat-designer-load-flow; went LIVE 2026-09-01 — see feat-design-2) | Spec + 16-task plan → subagent-driven build with per-task reviews + two whole-branch reviews; migrations 0048 (job_designers, design columns, design_scores), 0049 (designer files RLS, protected JO bucket), 0050 (created_by, coordinator/production RLS). Design brief card + AI scoring (Haiku/Sonnet, baseline table, real-change triggers, `after()` dispatch) + due-date auto-shift/keep-or-shift prompt + Design Load board (bottom-anchored, full-width, side bubble, Board/My Jobs) + rating slider with trust check + daily cron/3-day reminders + admin AI Scores tab. Feedback rounds: coordinator suggest-only everywhere, sales-level coordinator access, created_by, production file access, mobile hamburger drawer, draggable FABs, notification upgrades, zh fixes, designer grid into the brief card. Reviews caught ~15 real bugs pre-test (dead upload whitelist, silent delete no-op, RLS rename trap, lost clash pre-flight…). Nic re-test: 13/14 cleared; B3 + edits 15/16 queued. | [feat/feat-design-20260828-1-note.md](feat/feat-design-20260828-1-note.md) |

| fix-auth [Nic] | Google First-Login Bounce + Middleware Switched On | Root cause: stale session cookie → the callback client's background refresh fails → `_removeSession` wipes the PKCE code-verifier mid-exchange → `?error=auth`; retry clean because that response cleared the stale cookie. Found underneath: root `middleware.ts` never ran (must be `src/middleware.ts`). Callback drops stale session cookies first + logs failure reasons; middleware moved into `src/`, skips `/api/*`/`/ext/*`/`/auth/*`/`/mockups/*`, redirects carry cookies; login page shows `?error=` messages; 2 new standalone suites (29 checks), all 11 green. Verified by curl probes on local, preview (via the new Vercel bypass secret) and production; dev→main same day. | [fix/fix-auth-20260831-1-note.md](fix/fix-auth-20260831-1-note.md) |

| feat-design-2 [Nic] | Design Load — Fix Round 3 + LIVE on Production | B3 root-caused from the DB timeline, not code: the earlier-move test's only designer (Wan Jun) has no Telegram chat id, the later move hit the earlier-only rule → Telegram on every due-date shift + `[jobs/patch] …` logging (the silent catch now reports). Edits 15–19 built + Nic re-tested on the branch preview: edge-snapping FABs (judged against the laid-out viewport width so a right-snapped button stays right on resize), override-modal copy, four designer due-date notification types — moved / install moved with due kept / removed / set-changed — bell + Telegram, no migration; the "cleared due date comes back" fix (route echoed `updates ?? job` → old date; `\|\| null` guards '' dates; a clear beats the auto-shift in the same save). Merged clean-cut `feat-designer-load-flow` → `dev` (4e90c7b, 3 doc conflicts resolved dev-side) → `main` (3577064); production probes green (new API 401, pages 307 → login, crons 403/401, ext 200). Test data wiped with a dry-run-first one-off script: 9 test jobs + all child rows + 10 R2 objects (13.5 MB); the dry run caught 8 real client jobs that "wipe all" would have taken — kept. Worktree removed, branch kept for history. All 14 suites green (232 checks). | [feat/feat-design-20260901-1-note.md](feat/feat-design-20260901-1-note.md) |

| feat-workflow-v3 [Nic] | Workflow V3 Round 1 — Project Containers (BUILT on feat-workflow-v3, NOT merged) | Spec settled through 5 mockup rounds + Nic's artifact comments; 14-task plan run subagent-driven (fresh implementer + reviewer per task; whole-branch final review caught project-files 404-on-open and inheritance-severing saves). Migration 0051 (`job_projects`, `jobs.project_id`/`time_inherited`, project buckets/files + RLS); 3 new standalone suites (timing inheritance, date-range merge, picker keywords — 19 suites total green); 5 project routes incl. atomic bulk push + one-summary `tplProjectPushed`; AddJobPicker; ProjectFormShell (new/edit; read-only for designer/production); Multiple-jobs switch + prefill; project files under `projects/`; /projects pages; assistant Projects → **Workspaces**. Smoke round cleared with 5 fixes: autosave before new-job-in-project, amber notice, project push reuses the job clash modal (one Telegram kept), "(Untitled X)" guard, coordinator pending-sharing rule. Merge deferred until rounds 2–3 are built (Nic). | [feat/feat-workflow-20260902-1-note.md](feat/feat-workflow-20260902-1-note.md) |

| feat-provision [Nic] | Provisioning Overhaul — Card-Only Users, Subroles, Driver, Qualifications, Support Crew (LIVE for launch day) | Launch-eve build in its own worktree off dev (dev tree busy with the tour agent). Migration **0052** (renumbered — V3's unmerged branch had already applied 0051 to the shared DB; new memory + checklist rule: check every branch before numbering): `users.subrole` / `is_driver` / `qualifications` + 0044 guard extended. Card-only provisioning (email optional, attach later, callback linking untouched); admin rename + email editing; role/subrole filter bar with "No subrole"; link-status DOTS (red/amber/green beside the name — Nic's artifact comments replaced tag chips) + Driver chip + subrole/qualification chips on admin rows + installer/designer grids; Insert-button tag inputs; years/skills hidden everywhere (columns kept, drop ledgered); FCFS + clash meta swapped; **Support crew** = sub-installer bucket widened to all roles on the existing `is_sub_installer` flag (Supporting-Role Telegram, clash-exempt, zero extra migration — both save routes verified role-agnostic). Vercel gotcha: branch name over the 63-char DNS limit → preview URL can't exist; renamed to `feat-provision` + empty commit to re-trigger. New `user-meta` suite (TDD); 19 suites + build green; merged with the tour work (clean), dev → main `fc9f13c`, production probes green (login 200, pages 307, admin API 403, `sin1`). | [feat/feat-admin-20260904-1-note.md](feat/feat-admin-20260904-1-note.md) |

| infra-perf [Nic] | Page Navigation Speed — Singapore Region + Loading Skeletons | Nic's "few seconds between pages" root-caused with live probes: `X-Vercel-Id: sin1::iad1` — functions on Vercel's default US East region since day one, while Supabase answers ~20ms away in Singapore; each navigation = 5–6 sequential cross-Pacific round trips (middleware getUser + deleted_at check; page getUser + profile + jobs + follow-up sales names) with zero loading.tsx anywhere (taps froze on the old page). Hydration #418 ruled out — unrelated, untouched per CLAUDE.md. Fixes: `"regions": ["sin1"]` in vercel.json; loading.tsx on 10 routes (schedule / pending / completed / fcfs / design-load / installer / assistant / admin / jobs [id] + new) rendering a shared `PageSkeleton` (tabs/detail variants mirroring the CompanyBar + BottomNav frame). Type-check + build green. Branch preview probed via the bypass secret: `sin1::sin1`, ~0.09–0.14s vs prod 0.31–0.40s on /login; Nic approved, rebased over the tour agent's dev docs commits → `dev` → `main` (70668f3), production probed `sin1::sin1` post-deploy. Temp branch + worktree deleted (Nic — no archive). Residue: first tap after idle still pays a Hobby-plan cold start. | [infra/infra-perf-20260903-1-note.md](infra/infra-perf-20260903-1-note.md) |

| feat-tour [Nic] | Guided App Tour — 6 Roles, 3 Languages (LIVE) | Brainstormed → spec → interactive sales-role mockup approved before any code (artifact, EN/中文/বাংলা toggle; building it caught a phone-nav plan bug) → 12-task subagent-driven build. Pure engine + placement helper + TourProvider in CompanyBar; data-tour anchors across the shells; welcome card → language chooser (sets the app language via the existing route — the tour's only write); 6 scripts closing on Connect Telegram + "You're all set!"; ~84 bn strings (Nic's scoped bn-freeze exception, unvetted, demo corrections pending); 3 new standalone suites (18 total green). Final whole-branch review caught a stale-cross-role-state navigation loop (Critical), a 2s undimmed gap between steps, two data-dependent anchors, and impossible checklist items — one fix wave, all verified. Nic previewed, skipped the full tick-through (his call), merged dev→main — the merge also shipped the concurrent provisioning-overhaul agent's work after a live-DB probe confirmed migration 0052 applied (its gate was unticked; an unapplied 0052 would have crashed production). Post-demo polish parked: Escape/ARIA, Back-through-route, z-index headroom, language double-tap guard. Nic's new standing instruction mid-session: work inline, no subagent dispatches unless explicitly re-approved. | [feat/feat-tour-20260904-1-note.md](feat/feat-tour-20260904-1-note.md) |

| feat-installer [Nic] | Installer Completion Flow + Colour-Class Sweep (LIVE) | Completed button for installers on the job form — photo-gated grey → brand-green, confirm modal, new POST `/api/jobs/[id]/complete` (installer-only via getEffectiveRole; formal assignment + scheduled + ≥1 completion photo checked on the service client — installers have no jobs UPDATE RLS; idempotent double-tap; awaited `tplJobCompleted` Telegram to the sales POC). "Signed DO (Optional)" hidden until `do_issued` (or files already exist) — matches the installer-view "No DO Required" status. Migration **0053**: installers SELECT all completed jobs + files/messages/tasks/formal crew (`job_is_completed()` SECURITY DEFINER breaks the jobs↔job_assignees policy recursion; download-url route auto-widens since it authorizes by jobs RLS); `getInstallerJobs` trimmed to scheduled-only. Nic's "button invisible in daylight" report exposed that `bg-green` isn't a real class → app-wide sweep renamed **30 bare green/amber/blue utilities in 9 files** to `brand-*` (never compiled since written; verified against built CSS; numbered shades untouched). New completion-rules suite (11 checks), 20 suites green; Nic tested as a real installer on the dev preview; Claude ran the 0053 db push at Nic's request; dev → main `edeaf62`, production probes green (complete-route 401 / login 200 / schedule 307). | [feat/feat-installer-20260904-1-note.md](feat/feat-installer-20260904-1-note.md) |

> Archived notes are in `docs/pre-rebase-notes/`.

---

## Session 19

**Pre-Alpha Testing (Myself)** _(PASSED clean 2026-08-17 [Nic])_

Solo end-to-end run through every flow (sales → scheduler → installer → completion). **Result: clean pass — no issues found.** Version stays at **V.0.0.0.1** (no fixes were needed).

---

## Session 20

**Pre-Alpha Feedback** _(skipped 2026-08-17 — clean pass left nothing to fix)_

Session 19 surfaced no findings, so no hotfix round was needed. Green light given to bring in the scheduler — proceed to Session 21.

---

## Sessions 21–23

**Alpha / Beta / Launch** _(closed 2026-08-18 — webapp declared LAUNCHED **v1.0.0** by Nic)_

Nic confirmed all necessary testing was already done, so the planned alpha (scheduler), beta (management) and formal launch-cutover rounds were closed without being run. The current production deployment (greenqubes-ops.vercel.app, existing Supabase project, existing Telegram webhooks) **is** the launched product. No prod-tier promotion or custom domain was wired — those remain available as future infra tasks if ever wanted. The webapp continues as the desktop/office tool.

---

## Mobile App

**The new roadmap** _(spec approved + committed 2026-08-18 — see [superpowers/specs/2026-08-18-mobile-app-design.md](superpowers/specs/2026-08-18-mobile-app-design.md))_

Native Android + iPhone app, React Native + Expo, one codebase in `mobile/`, same backend. Nic reviews the spec, then an implementation plan gets written, then build sessions begin.

- **Stage 1 — Communication core:** login (Google + new email sign-in link, also added to the webapp), job list/detail, per-job chat at WhatsApp quality, push notifications, bell. App versioning starts at V.0.X.0.0.
- **Stage 2 — Field work:** installer dashboard, tasks, photo/DO uploads, offline read + queued sync.
- **Stage 3 — Office on the go:** schedule list, create/push jobs with clash warnings, assign installers, AI assistant, in-app digest vote. App becomes **V.1.0.0.0**.

Android rolls out first (direct .apk, free). iPhone waits on the directors' Apple Developer greenlight (US$99/yr; D-U-N-S number first, weeks of lead time). **Telegram bots retire only when Stage 3 AND the iPhone rollout are both live.** Admin + FCFS stay desktop-only; externals keep their web links.

---

## Environment Status

| Key | Status |
|---|---|
| Supabase URL + anon key + service role | ✓ in `.env.local` |
| Cloudflare R2 | ✓ in `.env.local` |
| Cloudflare Images | ✓ API token in `.env.local` — delivery URL pending |
| Anthropic | ✓ in `.env.local` |
| Voyage AI | ✓ in `.env.local` |
| Telegram bot | ✓ in `.env.local` |
| Vercel | ✓ connected to GitHub, auto-deploy live — preview at https://greenqubes-ops.vercel.app |
| Server PC (backup) | ✓ rclone + Task Scheduler configured, nightly running |
